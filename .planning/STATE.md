---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: "Phase 3 Wave 3 engine landed (Plan 03-04). Plans 03-05 (metrics + audit) and 03-06 (CLI harness) are Wave 3 sibling plans that consume `receptra.llm.engine` surface unchanged: Plan 03-05 imports LlmCallTrace and wires record_call to log_llm_call+insert_llm_call; Plan 03-06 iterates the AsyncGenerator. Next: Plan 03-05. Phase 2 deferred work (numeric WER + latency baseline on reference M2 hardware) still pending — independent of Phase 3."
last_updated: "2026-04-26T16:00:20.316Z"
progress:
  total_phases: 7
  completed_phases: 2
  total_plans: 18
  completed_plans: 17
  percent: 94
---

# Receptra — STATE.md

*Project memory. Updated at phase transitions and plan boundaries.*

## Project Reference

- **Name:** Receptra
- **Core Value:** A human agent taking a Hebrew call on a Mac gets useful, grounded suggestions in under two seconds — running entirely on their own machine with no cloud dependency.
- **Milestone:** M1 — Hebrew Co-pilot MVP
- **Exit Criterion:** Live Hebrew demo end-to-end on reference Apple Silicon (M2 16GB or M2 Pro 32GB), fully offline.
- **Granularity:** standard
- **Mode:** yolo
- **Model Profile:** quality

## Current Position

Phase: 03-hebrew-suggestion-llm — Wave 3 engine + metrics/audit shipped (Plans 03-04 + 03-05). LLM-05 instrumentation surface published; ready for Plan 03-06 (CLI harness).

- **Phase:** 03-hebrew-suggestion-llm (5/6 plans complete)
- **Plan:** 03-04 complete (Wave-3 — `receptra.llm.engine.generate_suggestions(transcript, context_chunks, *, request_id=None, model=None, record_call=None) -> AsyncGenerator[SuggestionEvent, None]` published as the LLM-02 internal interface. Five paths from a single function: hard short-circuit on empty context OR whitespace transcript → canonical refusal CompleteEvent BEFORE any Ollama call (LLM-03 strongest grounding defense, RESEARCH §5.5 layer 1, saves ~2s); happy path → TokenEvents + parsed CompleteEvent with measured TTFT (perf_counter monotonic); parse-retry → ONE-shot bounded retry via Plan 03-03 retry_with_strict_json on pydantic.ValidationError or json.JSONDecodeError (recovery yields CompleteEvent with status='parse_retry_ok'; exhaustion yields LlmErrorEvent + canonical refusal CompleteEvent); ollama_unreachable → OllamaUnreachableError or httpx.ConnectError yields LlmErrorEvent only; timeout → httpx.ReadTimeout yields LlmErrorEvent(code='timeout'). DoS bounds from Plan 03-02 propagate: ValueError from build_user_message caught and yielded as LlmErrorEvent(code='no_context'). Streaming chat call deliberately omits format=<schema> kwarg (Pitfall A / RESEARCH §3.5 / upstream issues #14440+#15260); only retry uses format='json' loose with stream=False. OllamaModelMissingError collapsed onto wire-level ollama_unreachable code with model_missing: prefix in detail — keeps LlmErrorEvent.code Literal narrow at 4 values; LlmCallTrace.status carries granular 7-value audit status for Phase 7 analysis. LlmCallTrace frozen dataclass exposes per-call structural data via record_call callback hook (default no-op; Plan 03-05 wires it to log_llm_call+insert_llm_call). Engine NEVER calls loguru/sqlite3 directly — keeps engine tests offline+writable-DB-free. record_call invocation wrapped in contextlib.suppress(Exception) in finally — defense in depth even though Plan 03-05's record_call also swallows internally. _strip_markdown_fences pure helper handles ```json/```/triple-backticks; idempotent; preserves mid-body backticks; applied BEFORE pydantic parse on both first stream + retry response. _CANONICAL_REFUSAL Suggestion(text='אין לי מספיק מידע', confidence=0.0, citation_ids=[]) byte-exact from RESEARCH §5.5/§5.4. 24 mocked-engine behavioral tests covering all five paths + 6-case parametrized fence-strip + 3 callback invariants + Pitfall-A regression + Pitfall-C concurrency regression (two concurrent calls <250ms vs ~300ms serialized). 2 opt-in @pytest.mark.live tests gated behind RECEPTRA_LLM_LIVE_TEST=1: structural smoke + grounding-refusal contract (irrelevant context → 'אין לי מספיק מידע'). Full backend suite 169 pass / 5 skip; ruff + mypy strict clean across 44 source files. 2 deviations auto-fixed: (1) Rule 1 — cross-test sys.modules pollution from tests/llm/test_client.py's STT-isolation save+restore caused string-path monkeypatch.setattr('receptra.llm.engine.X', ...) to fail with AttributeError under full-suite alphabetical ordering; fix establishes new project pattern: when sibling tests mutate sys.modules, use module-reference monkeypatch.setattr(engine_module, 'attr', new) two-arg form to bypass derive_importpath getattr-walk; (2) Rule 3 — ruff RUF022/I001 import-sort cleanup. Both auto-fixed inside the GREEN commit.)
- **Status:** Phase 3 Wave 3 metrics + audit landed (Plan 03-05). LLM-05 instrumentation surface published: `receptra.llm.metrics.build_record_call(audit_path)` factory composes `log_llm_call` (loguru `event="llm.call"` with PII redaction default-on, mirrors `stt_log_text_redaction_disabled` byte-for-byte) + `insert_llm_call` (stdlib sqlite3 idempotent CREATE TABLE IF NOT EXISTS llm_calls + 2 CREATE INDEX IF NOT EXISTS, RESEARCH §6.4 schema verbatim, co-exists with Phase 2 stt_utterances on same data/audit.sqlite). Engine surface unchanged. Next: Plan 03-06 (CLI harness — consumes `build_record_call(settings.audit_db_path)` directly, re-verifies STT-isolation at integration boundary). Phase 2 deferred work (numeric WER + latency baseline on reference M2 hardware) still pending — independent of Phase 3.
- **Progress:** [█████████░] 94%

```
[█████████░] 94% — 17/18 plans (Phase 1: 6/6; Phase 2: 6/6; Phase 3: 5/6)
```

## Performance Metrics

| Phase | Plan | Duration | Tasks | Files | Completed |
|-------|------|----------|-------|-------|-----------|
| 01-foundation | 01-01 | 3min | 3 | 10 | 2026-04-23 |
| 01-foundation | 01-02 | ~9min | 3 | 11 | 2026-04-23 |
| 01-foundation | 01-03 | ~20min | 2 | 16 | 2026-04-24 |
| 01-foundation | 01-04 | ~15min | 3 | 6 | 2026-04-24 |
| 01-foundation | 01-05 | ~8min | 2 | 5 | 2026-04-24 |
| 01-foundation | 01-06 | ~2min | 2 | 3 | 2026-04-24 |
| 02-hebrew-streaming-stt | 02-01 | ~8min | 2 | 5 | 2026-04-24 |
| 02-hebrew-streaming-stt | 02-02 | ~12min | 2 | 13 | 2026-04-25 |
| 02-hebrew-streaming-stt | 02-03 | ~4min | 1 | 3 | 2026-04-25 |
| 02-hebrew-streaming-stt | 02-04 | ~10min | 2 | 6 | 2026-04-25 |
| 02-hebrew-streaming-stt | 02-05 | ~10min | 2 | 9 | 2026-04-25 |
| 02-hebrew-streaming-stt | 02-06 | ~8min | 2 | 12 | 2026-04-25 |
| 03-hebrew-suggestion-llm | 03-01 | 3min | 2 | 8 | 2026-04-25 |
| 03-hebrew-suggestion-llm | 03-02 | 12min | 2 | 6 | 2026-04-26 |
| 03-hebrew-suggestion-llm | 03-03 | 5min | 1 | 2 | 2026-04-26 |
| 03-hebrew-suggestion-llm | 03-04 | ~7min | 2 | 3 | 2026-04-26 |
| 03-hebrew-suggestion-llm | 03-05 | ~7min | 2 | 4 | 2026-04-26 |

- Phases completed: 2/7 (Phase 1 Foundation complete; Phase 2 Hebrew Streaming STT complete; Phase 3 Hebrew Suggestion LLM in progress)
- Plans completed: 17 (Phase 1: 6/6; Phase 2: 6/6; Phase 3: 5/6)
- v1 requirements delivered: 20/42 (Phase 1 FND-* complete + STT-01..06 complete + LLM-01 client layer complete + LLM-02 engine + LLM-03 grounding + LLM-04 parse contracts complete via Plan 03-04 + LLM-05 metrics + audit instrumentation complete via Plan 03-05)

## Accumulated Context

### Decisions (locked)

- Co-pilot first (no TTS, no telephony in v1)
- Hebrew-first (ivrit.ai + DictaLM 3.0 primary, Qwen 2.5 7B fallback)
- Apple Silicon M2+ as reference floor
- OSS self-host (Apache 2.0), Docker Compose one-liner
- STT: faster-whisper + ivrit-ai/whisper-large-v3-turbo-ct2 + Silero VAD
- LLM runtime: Ollama with Metal
- Embeddings: BGE-M3 via Ollama; Vector DB: ChromaDB
- Frontend: React + Vite + TypeScript + Tailwind (RTL)
- Parallelizable: Phases 2, 3, 4 after Phase 1
- Backend dependency manager: uv (0.11.7+) with pyproject.toml + uv.lock (Plan 01-02)
- Backend build backend: hatchling (PEP-621) with src-layout wheel (Plan 01-02)
- Backend config pattern: pydantic-settings Settings with RECEPTRA_ env prefix, extra="ignore", .env-file support (Plan 01-02)
- Backend lint+type gates: strict mypy (src + tests) + ruff E/F/I/N/UP/B/C4/SIM/RUF; enforced in CI (Plan 01-02 → Plan 01-06)
- Frontend stack: React 19 + Vite 6 + TypeScript 5.6 + Tailwind v4 (`@tailwindcss/vite` plugin, no tailwind.config.js) — Plan 01-03
- Frontend RTL: `<html dir="rtl" lang="he">` on root + defense-in-depth attrs on `<main>` in App.tsx; Phase 6 layers on `hebrew-tailwind-preset` — Plan 01-03
- Frontend dev proxy: Vite forwards `/api/*` (HTTP) and `/ws/*` (WebSocket, `ws: true`) to `localhost:8080`; `host: '0.0.0.0'` + `strictPort: true` for Docker exposure — Plan 01-03
- Frontend lint+format gates: ESLint 9 flat config + Prettier 3 (singleQuote for JS/TS, double-quote CSS override); consumed by Plan 01-06 CI — Plan 01-03
- Frontend scripts contract pinned: dev / build / preview / lint / typecheck / format / format:check — Plan 01-06 CI calls these names (Plan 01-03)
- Ollama runs HOST-native, NOT in docker-compose — OPEN-1 locked. Backend reaches it via `host.docker.internal:11434` with `extra_hosts: host-gateway` for Linux parity (Plan 01-04)
- Docker Compose stack = chromadb + backend + frontend with healthcheck-gated chain (`depends_on.condition: service_healthy`); `docker compose config -q` is the CI static gate (Plan 01-04)
- Container images: non-root `receptra` user (uid 1001) in both backend (python:3.12-slim multi-stage + uv) and frontend (node:22-slim, Vite dev server in Phase 1; nginx static serve deferred to Phase 7) — Plan 01-04
- Model weights mounted read-only (`${MODEL_DIR:-~/.receptra/models}:/models:ro`) into backend container — never baked into images (Plan 01-04)
- `chromadb/chroma:1.5.8` image pinned; healthcheck on `/api/v2/heartbeat`, volume at `/data` (Plan 01-04)
- OPEN-2 LOCKED: default DictaLM quant is Q4_K_M (16GB M2 reference hardware); Q5_K_M override via `DICTALM_QUANT` env var for 32GB+ Macs (Plan 01-05)
- OPEN-1 ENFORCED in Makefile: `make up` pgrep-gates `ollama serve` on host (via `nohup` + /tmp log), then `docker compose up -d`. Ollama never enters compose (Plans 01-04 + 01-05)
- Model downloads are separate from `docker compose up`: `make models` delegates to `scripts/download_models.sh` using hf CLI + ollama pull, resumable, ~11 GB total (FND-03, Plan 01-05)
- DictaLM Ollama registration uses a Modelfile TEMPLATE with `__GGUF_PATH__` sed-substitution at ollama-create time — checked-in template is repo-portable, absolute paths resolved per contributor (Plan 01-05)
- License allowlists single-source-of-truth in `scripts/check_licenses.sh`: verbatim research §5.4 (pip-licenses, both SPDX + long-form names) + §5.5 (license-checker, SPDX). `make licenses` + Plan 01-06 CI both invoke this script (Plan 01-05)
- OPEN-6 LOCKED: CI runner = ubuntu-latest only in Phase 1. Mac-native `docker compose up` / Metal / arm64 wheel smoke deferred to Phase 7 (Plan 01-06)
- OPEN-8 LOCKED: license-gate negative test = manual workflow_dispatch only (`.github/workflows/license-gate-test.yml`). Installs GPLv3 `gnureadline` into scratch venv + asserts pip-licenses allowlist rejects it. Not on every push (slows CI, pollutes caches) (Plan 01-06)
- OPEN-1 ENFORCEMENT moved from docs + Makefile into CI: compose job grep-fails on `^\s*ollama:` in docker-compose.yml (Plan 01-06 — mitigates T-01-06-04 silent regression)
- CI topology locked: 4 parallel jobs on ubuntu-latest (backend, frontend, compose, licenses) gating every push + pull_request; concurrency.cancel-in-progress for superseded runs; actions pinned at @v4 (Plan 01-06)
- Plan 02-01: Phase 2 runtime deps pinned in backend/pyproject.toml — faster-whisper 1.2.1 (MIT), silero-vad 6.2.1 (MIT), numpy 2.0+ (compound permissive), loguru 0.7.3 (MIT). Dev deps: jiwer 4.0 (Apache-2.0), soundfile 0.13 (BSD). Transitive: ctranslate2 4.7.1, torch 2.11.0, onnxruntime 1.25.0, tokenizers 0.22.2, av 17.0.1. All permissive; license gate passes (Plan 02-01)
- Plan 02-01: STT contract partial_interval_decision=700 ms (provisional) — scripts/spike_stt_latency.py embeds decision rule; .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md is the durable contract Plans 02-02..02-06 consume; Plan 02-06 re-runs on reference M2 hardware before Phase 2 exits to replace provisional value with measurement (Plan 02-01, OPEN-2 resolved for STT)
- Plan 02-01: scripts/check_licenses.sh PY_ALLOW extended with 5 pip-licenses 5+ verbatim strings (all pre-existing transitive deps): `Apache-2.0 OR BSD-2-Clause` (packaging), `3-Clause BSD License` (protobuf), `ISC License (ISCL)` (shellingham), `MPL-2.0 AND MIT` (tqdm), `BSD-3-Clause AND 0BSD AND MIT AND Zlib AND CC0-1.0` (numpy). JS side: `--excludePackages receptra-frontend@0.1.0` excludes private workspace self-reference (Plan 02-01, OPEN-4 resolved)
- Plan 02-01: Whisper pinned kwargs are the single-source contract surface — WhisperModel(device=cpu, compute_type=int8, cpu_threads=4, num_workers=1) + transcribe(language=he, task=transcribe, beam_size=1, best_of=1, temperature=0.0, condition_on_previous_text=False, vad_filter=False, without_timestamps=True). Spelled out inline in scripts/spike_stt_latency.py; downstream plans reuse these values verbatim (Plan 02-01)
- Plan 02-01: Spike-with-fallback pattern — when model weights absent, spike script exits 1 AND writes UNMEASURED placeholder with provisional default; downstream plans never blocked by missing-model executor (Plan 02-01)
- Plan 02-02: FastAPI lifespan = asynccontextmanager that loads WhisperModel + Silero VAD singletons onto app.state and runs warmup transcribe BEFORE yield. `@app.on_event` fully removed from main.py (Pitfall #1 mitigated; greptest in test_lifespan.py guards against regression). Pitfall #7 mitigated via warmup transcribe through the same `transcribe_hebrew()` wrapper the hot path uses (Plan 02-02)
- Plan 02-02: `receptra.stt.engine.transcribe_hebrew(model, audio_f32) -> (text, info_dict)` is the single source of truth for RESEARCH §7 Hebrew transcribe kwargs. Threat T-02-02-01 (kwargs drift) mitigated via 5 unit tests pinning every locked kwarg by name+value. Float32-only dtype gate raises TypeError before model invocation (Pitfall #4 defense) (Plan 02-02)
- Plan 02-02: Loguru is now the single backend logging sink with serialize=True JSON output — replaces stdlib logging.basicConfig in main.py. Establishes the audit-log JSON event shape Phase 5 INT-05 will consume; structured events emitted via `logger.bind(event="stt.lifespan").info({...})` (Plan 02-02)
- Plan 02-02: Settings extended with 8 STT-related fields (whisper_model_subdir, whisper_compute_type=int8, whisper_cpu_threads=4, audit_db_path, stt_partial_interval_ms=700 honoring 02-01-SPIKE-RESULTS.md provisional lock, vad_threshold=0.5, vad_min_silence_ms=300, vad_speech_pad_ms=200) — all documented in .env.example (Plan 02-02)
- Plan 02-02: Test-time stub-before-import pattern — `backend/tests/conftest.py` autouse `_stub_heavy_loaders` monkeypatches WhisperModel + load_silero_vad before any app import; STT-specific tests opt into a richer stub fixture (`stubbed_app` in test_lifespan.py) when introspection is needed. Keeps CI offline + fast and works with TestClient lifespan startup (Plan 02-02)
- Plan 02-02: mypy strict override `ignore_missing_imports=true` for `faster_whisper` + `silero_vad` modules (no py.typed markers upstream); the wrapper boundary uses `Protocol` to keep our public surface narrowly typed (Plan 02-02)
- Plan 02-03: `receptra.stt.vad.StreamingVad(model, threshold, min_silence_ms, speech_pad_ms)` is the per-connection VAD wrapper. Each WebSocket handler in Plan 02-04 constructs its OWN StreamingVad wrapping the shared `app.state.vad_model` singleton; the underlying `silero_vad.VADIterator` carries per-session state and `reset_states()` is called in BOTH `__init__` AND the public `reset()` method (Pitfall #2 belt-and-braces; regression-tested by `test_two_instances_have_independent_state`) (Plan 02-03)
- Plan 02-03: Wire-format constants `FRAME_BYTES=1024`, `FRAME_SAMPLES=512`, `SAMPLE_RATE_HZ=16000` exported from `receptra.stt.vad` as module-level symbols. Plan 02-04 imports them so the WebSocket binary-frame size guard and the VAD wrapper share a single source of truth. Little-endian explicitly pinned via `dtype="<i2"` (NOT `np.int16`, which would honor native byte order) — Pitfall #4 partial defense; full byte-order validation is at the WebSocket protocol layer in Plan 02-04 (Plan 02-03)
- Plan 02-03: `InvalidFrameError(ValueError)` raised by `StreamingVad.feed()` BEFORE any numpy allocation when frame length != 1024 bytes. Plan 02-04 converts this to `{"type":"error","code":"protocol_error"}` envelope and closes the WebSocket. Threat T-02-03-01 (DoS via malformed frame size) mitigated; ASVS V5 input validation (Plan 02-03)
- Plan 02-03: `StreamingVad` accepts an already-loaded model + segmentation params via constructor — does NOT import `receptra.config` and does NOT call `load_silero_vad`. Decouples module from Plan 02-02 lifespan, makes unit tests self-contained, matches the FastAPI canonical app.state dependency-injection pattern. Plan 02-04 wires settings at the WebSocket entry point (Plan 02-03)
- Plan 02-03: `VadEvent` TypedDict normalizes `silero_vad.VADIterator`'s `{"start": seconds_float}` / `{"end": seconds_float}` raw output into `{"kind": "start"|"end", "t_ms": int}` for downstream consumers. Plan 02-06 uses `t_ms` timestamps as the headline latency metric (speech-start to first-partial-transcript) (Plan 02-03)
- Plan 02-03: Test signal upgraded from pure-tone to FM-modulated harmonic stack with 5 Hz AM envelope and 0.15-amplitude broadband noise — pure 1 kHz sinusoids register as silence to Silero v5 (peak prob ~0.2); the formant-FM synthesizer reliably crosses 0.9. Noise component seeded by phase (deterministic function of frame index, NOT a stateful module RNG) so test ordering is irrelevant (Plan 02-03)
- Plan 02-04: `receptra.stt.events` publishes the wire schema as a pydantic v2 discriminated union (SttReady, PartialTranscript, FinalTranscript, SttError) on the `type` field; every model `frozen=True`; `SttEvent = Annotated[... | ..., Field(discriminator='type')]` is the TypeAdapter alias Plan 02-05/02-06 + Phase 6 frontend parse against. SttError code is a 3-value Literal allowlist (model_error/vad_error/protocol_error) — adding a 4th requires plan amendment so consumer switches stay total (Plan 02-04)
- Plan 02-04: `receptra.stt.pipeline.run_utterance_loop(ws, vad, transcribe)` is the wrappable unit Plan 02-06 metrics middleware will instrument; the FastAPI route stays a thin shim in main.py. The transcribe parameter is `Callable[[NDArray[np.float32]], Awaitable[str]]` so the threadpool/instrumentation strategy is hidden behind the closure — `websocket_stt_endpoint` puts the `await asyncio.to_thread(transcribe_hebrew, ...)` wrap inside its inner closure (Pitfall #5 mitigated; regression `test_no_event_loop_blocking` drives 2 concurrent WS w/ 200ms sleep stub) (Plan 02-04)
- Plan 02-04: Partial cadence is gated on ACCUMULATED AUDIO TIME (`audio_ms_since_partial >= settings.stt_partial_interval_ms`), NOT wall-clock time. RESEARCH §7's wall-clock pseudocode fires correctly under real-time streaming (1 s wall ≈ 1 s audio buffered) but never fires when a client bursts frames faster than real-time (TestClient + any non-real-time producer). Audio-time gating is also semantically more correct: the user-facing contract is "one partial per N ms of speech." Documented inline in run_utterance_loop with multi-line rationale comment (Plan 02-04, Rule 1 deviation from research)
- Plan 02-04: WebSocket close code 1007 (RFC 6455 Invalid Frame Payload Data) on the protocol_error path — semantically correct over generic 1000. Frontend can switch on the close code to distinguish "bad client" from "normal disconnect" (Plan 02-04)
- Plan 02-04: Per-connection StreamingVad construction inside `websocket_stt_endpoint` wraps the SHARED `app.state.vad_model` singleton; `vad.reset()` in `finally` even though wrapper is local-scope — defense-in-depth + documents the per-connection isolation contract for future refactors (Pitfall #2). Cleanup ladder: `WebSocketDisconnect` → clean log; `Exception` → `SttError(model_error)` envelope + log w/ stack; always vad.reset() + ws.close() suppressed via contextlib.suppress (Plan 02-04)
- Plan 02-04: Test fixtures `real_vad_app` / `real_vad_canned_whisper_app` / `real_vad_slow_whisper_app` override the autouse conftest VAD stub with the real Silero TorchScript model (small, ~30MB, already in dev cache). Whisper stays stubbed (`_CannedWhisperStub` returning `iter([_Segment(' שלום')])` or sleep-stub variant). Necessary because StreamingVad → VADIterator calls model.reset_states() which the autouse `object()` sentinel does not satisfy. Stubbed Whisper avoids loading 1.5 GB into the test runner (Plan 02-04)
- Plan 02-05: `receptra.stt.wer` publishes `normalise_hebrew` (NFC + niqqud/bidi → empty + punctuation → space + whitespace collapse) + `compute_wer` (jiwer 4.0 wrapper). Two-class regex split is mandatory — RESEARCH §9 single-class would shatter "שָׁלוֹם" (3 letters + 4 niqqud) into "ש ל ם" (3 tokens), breaking the test contract. Niqqud/bidi sit INSIDE word forms (intra-word → empty), punctuation sits BETWEEN words (between-words → space). jiwer Compose pipeline from RESEARCH §9 sketch is skipped — jiwer 4.0 rejects non-ListOfListOfWords transforms; `normalise_hebrew` already covers Strip + RemoveMultipleSpaces equivalents (Plan 02-05, Rule 1 + Rule 3 deviations from research)
- Plan 02-05: `scripts/fetch_stt_fixtures.py` uses the `datasets` library lazily (NOT in `pyproject.toml` — opt-in regen-only dep documented in `docs/stt-eval.md`). Uses pinned `CV_REVISION_SHA` module constant (T-02-05-01 mitigation). Probes auth via `HfApi().dataset_info(CV_REPO)` before `load_dataset` and emits 5-step unblock instruction on 401 because Common Voice 25.0 is a gated HF dataset. `--airgap-placeholder` mode writes 1-row UNFETCHED manifest + .gitkeep so regression test skips gracefully on CI executors without HF access. `trust_remote_code` stays False (T-02-05-05). No silent runtime dep additions (Plan 02-05)
- Plan 02-05: `scripts/eval_wer.py` imports `receptra.stt.engine.transcribe_hebrew` (no Hebrew kwarg duplication) — single source of truth design from Plan 02-02. JSON output schema `{status, baseline:{wer_mean,wer_median,wer_p95,cer_mean,cer_median,cer_p95,n_clips}, per_clip:[...]}` is stable for Plan 02-06 CI regression logging. `backend/tests/stt/test_wer_baseline.py` asserts measured ≤ BASELINE_WER + GRACE_PP (3pp); `GRACE_PP = 0.03` is a module-level constant per T-02-05-04 silent-grace-widening mitigation; OPEN-7 (WER informational, not pass/fail) honored — regression test catches dep-upgrade drift, not absolute accuracy floor (Plan 02-05)
- Plan 02-05: `BASELINE_WER` + `BASELINE_CER` intentionally `None` in this commit because the airgap fallback is active in this executor (HF gated + no `datasets` install + no Whisper weights on disk). Plan 02-06 phase-transition gate flags as follow-up: first contributor with HF access + Common Voice 25.0 license accepted + `make models` already run executes `scripts/fetch_stt_fixtures.py` + `scripts/eval_wer.py` and commits BASELINE_WER + BASELINE_CER + 30 real fixtures + a pinned `CV_REVISION_SHA` (currently `"main"`) in a single PR. STT-05 is structurally satisfied — the harness exists and is exercised end-to-end via the placeholder skip path; the numeric baseline is Plan 02-06 work (Plan 02-05)
- Plan 02-06: `receptra.stt.metrics` publishes `UtteranceMetrics` (frozen dataclass) with `stt_latency_ms` as a derived `@property` over monotonic timestamps — NOT a stored field, so the value cannot drift between the loguru log line and the SQLite audit row. `log_utterance(m)` emits one structured loguru JSON event `event="stt.utterance"` with all 11 metric fields. `new_utterance_id()` (uuid4 hex) + `utc_now_iso()` helpers are co-published. PII redaction is default-on: the `text` body is OMITTED from the log payload unless `settings.stt_log_text_redaction_disabled=True` (T-02-06-01 regression-tested both paths) (Plan 02-06)
- Plan 02-06: `receptra.stt.audit` is stdlib `sqlite3` only (no SQLAlchemy / aiosqlite — RESEARCH §Recommended Dependencies "Intentionally NOT added"). `init_audit_db(path)` is idempotent (`CREATE TABLE IF NOT EXISTS`), creates parent dirs lazily (T-02-06-06 mitigation for missing docker-compose `./data` host dir), and is invoked on every WS accept (cost ~µs). `insert_stt_utterance(path, m)` opens + closes a per-call connection inside a `with sqlite3.connect(...)` block — atomic per-block, no thread-safety hazard. Schema verbatim from RESEARCH §11; Phase 5 INT-05 extends via `ALTER TABLE ADD COLUMN` (Plan 02-06)
- Plan 02-06: `pipeline.py` `run_utterance_loop` tracks per-utterance `utterance_id` + `partials_emitted` (reset on every VAD-start, not on connection-start, because v1 supports multi-utterance per connection). After `send_json(FinalTranscript)` the loop builds `UtteranceMetrics` + calls `log_utterance` AND `insert_stt_utterance` — both wrapped in independent try/except so a logging or DB failure can NEVER crash the WS hot path. T-02-06-03 — only completed utterances reach the insert site so half-written rows are impossible by construction (regression-tested by `test_chaos_disconnect.py`) (Plan 02-06)
- Plan 02-06: `test_chaos_disconnect.py` is the RESEARCH §Validation Chaos contract regression guard. Negative test: open WS → 31 voiced frames (~1 s of audio) → abrupt close without silence trailer → assert chaos sequence completes <1000 ms (RESEARCH says <500 ms; 1000 ms ceiling absorbs TestClient teardown slack on CI), `stt_utterances` row count == 0, follow-up WS receives `ready` <1500 ms (event loop not wedged). Positive control: clean voiced burst → silence trailer → exactly 1 row written with Hebrew text byte-exact through SQLite (proves the chaos test cannot pass for the wrong reason). Both fixtures bind `settings.audit_db_path` to a `tmp_path` location so the test never touches the developer's `./data/audit.sqlite` (Plan 02-06)
- Plan 02-06: `docker-compose.yml` backend service gains `./data:/app/data:rw` volume + `RECEPTRA_AUDIT_DB_PATH=/app/data/audit.sqlite` env var. The existing `${MODEL_DIR}:/models:ro` mount is preserved unchanged. `.gitignore` evolves from `data/` to `data/*` with `!data/.gitkeep` exception so the host-bind dir survives fresh clones; also adds `backend/data/` catch-all for pytest side effects when tests run from `cd backend`. `data/.gitkeep` committed to reserve the directory (Plan 02-06)
- Plan 02-06: `docs/stt.md` (210 lines) is the user/contributor Phase 2 contract. 8 sections: Overview, Wire Contract, Event Schema, Latency Baseline (UNMEASURED on reference hardware — Plan 02-05 follow-up), Audit Log + PII Warning ("Do NOT attach to bug reports"), Running the WER Eval (links docs/stt-eval.md), Known Limitations (no Pipecat yet, no auth, CPU-only on Apple Silicon, partial flicker acceptable, multi-utterance per-connection best-effort), Troubleshooting (protocol_error / no partials / first-request-slow / log leak / clear audit log) (Plan 02-06)
- Plan 02-06: PHASE 2 COMPLETE — all 6 STT-* requirements landed across plans 02-02..02-06. Only deferred work is the numeric WER baseline + spike re-run on M2 reference hardware (carried forward from Plan 02-05 follow-up). Phase 2 is structurally and behaviorally complete; the missing baseline is data-only, blocked on contributor access to HF + Common Voice 25.0 license + reference Apple Silicon hardware (Plan 02-06)
- Plan 03-01: `ollama>=0.6.1,<1` (MIT) pinned in backend/pyproject.toml (uv.lock resolves to 0.6.1; httpx already in transitive surface from Phase 1; no new transitive licenses introduced). License gate re-runs clean — `ollama 0.6.1 MIT` enters PY_ALLOW with no allowlist edit needed (Plan 03-01)
- Plan 03-01: `Settings` extended with 9 LLM-tunable fields per RESEARCH §6.5 + §3.3 — every default LOCKED VERBATIM: `llm_model_tag="dictalm3"`, `llm_model_fallback="qwen2.5:7b"`, `llm_temperature=0.0` (grounding lock — Pitfall E mitigation, deterministic output enables אין לי מספיק מידע refusal contract), `llm_num_predict=512` (Hebrew text density vs Modelfile default 256 too tight for 3 cards), `llm_num_ctx=8192` (transcript ~200 + 5 chunks ~6000 + system + few-shot), `llm_top_p=0.9`, `llm_request_timeout_s=30.0` (absorbs ~5s cold-start), `llm_system_prompt_lang="he"` (DictaLM 3.0 is Hebrew-native; Phase 7 A/B; OPEN-LLM-2 lock), `llm_log_text_redaction_disabled=False` (PII default-on, mirrors stt_log_text_redaction_disabled exactly). All RECEPTRA_LLM_* env-tunable; documented in .env.example with PII-flag warning (Plan 03-01)
- Plan 03-01: `llm_system_prompt_lang` typed as `str` (NOT `Literal["he","en"]`) because pydantic-settings cannot load Literal types from .env without strict-mode shims; engine layer in Plan 03-02 prompts.py validates the value at consumption time — Settings stays simple and Plan 03-02 can extend the allowed set without revisiting this commit (Plan 03-01)
- Plan 03-01: Live-test triple gate (env var + marker + binary-on-PATH probe) locked OPEN-LLM-6. `@pytest.mark.live` registered in `backend/tests/llm/conftest.py` via `pytest_configure`; every live test starts with `if not live_test_enabled(): pytest.skip("set RECEPTRA_LLM_LIVE_TEST=1 to run live Ollama tests")`; ChatML grep gate adds two more skip predicates (`shutil.which("ollama") is None` + `dictalm3` not in `ollama list`). CI on ubuntu-latest stays green by self-skip; Mac dev flips one env var to exercise. This convention every Phase 3 plan inherits zero-cost (Plan 03-01)
- Plan 03-01: DictaLM 3.0 ChatML grep gate (Pitfall B + Assumption A5) ships as `test_dictalm3_chatml_template_detected` — runs `ollama show dictalm3 --modelfile` and asserts BOTH `<|im_start|>` AND `<|im_end|>` markers in stdout. Recovery path (explicit TEMPLATE block in scripts/ollama/DictaLM3.Modelfile) documented inline in test docstring so any failure is self-recoverable. Live test self-skipped on this executor (no host Ollama). First Mac contributor with `dictalm3` registered runs `RECEPTRA_LLM_LIVE_TEST=1 pytest tests/llm/` and the gate either confirms Assumption A5 or surfaces the recovery path (Plan 03-01)
- Plan 03-01: `backend/tests/llm/conftest.py` deliberately does NOT import `receptra.llm.*` (the package does not exist until Plan 03-02 — importing it would crash collection of every Phase 3 test). Plans 03-02..03-06 add fixtures here once the package is real; Wave 0 publishes only the marker + the `live_test_enabled()` helper (Plan 03-01)
- Plan 03-01: 4 new tests, 2 structural (dep-pin regression guard + skip-machinery proof) + 2 opt-in live; full backend suite goes 51+1 → 53+3 (no regression). Streaming + format=<schema> intentionally NOT used together (RESEARCH §3.5 — upstream ollama issues #14440, #15260; format constraint does not strictly hold mid-stream); Phase 3 will stream tokens for TTFT then parse JSON at done=true. This plan establishes only the dep + Settings + verification surface; engine itself is Plans 03-02..03-04 work (Plan 03-01)
- Plan 03-02: `receptra.llm.schema` publishes `Suggestion`/`SuggestionResponse` (frozen, extra='forbid', text 1-280 chars, confidence 0.0-1.0, suggestions 1-3 entries) + `TokenEvent`/`CompleteEvent`/`LlmErrorEvent`/`SuggestionEvent` discriminated union with pre-built `SuggestionEventAdapter` TypeAdapter — mirrors `stt.events.SttEvent` byte-for-byte so Phase 5 hot-path readers don't context-switch. `LlmErrorEvent.code` Literal narrow to 4 values (ollama_unreachable / parse_error / timeout / no_context); adding a 5th requires plan amendment so consumer switches stay total (Plan 03-02)
- Plan 03-02: `SYSTEM_PROMPT_HE` ships RESEARCH §5.2 verbatim (sha256 5726ca37a5ea082fee7b4b1b0dfe38c797d587a02f60ffea5324c9d62b341e0f, 765 chars / 1225 UTF-8 bytes); `SYSTEM_PROMPT_EN` is a placeholder English variant where the refusal phrase 'אין לי מספיק מידע' STAYS Hebrew so output language never drifts when system prompt is in English; `FEW_SHOTS_HE` always Hebrew regardless of `lang` parameter — model needs Hebrew exemplars to learn the JSON-shape contract. Phase 7 A/B owns real EN tuning (Plan 03-02)
- Plan 03-02: DoS bounds T-03-02-02 (transcript ≤2000 chars, ≤10 chunks, ≤12000 chars total body) enforced in pure-string `build_user_message`, NOT in engine. Keeps prompts.py I/O-free and unit-testable; Plan 03-04 catches the ValueError and yields LlmErrorEvent. Engine never sends a giant prompt to Ollama — Threat T-03-02-02 mitigated structurally before any Ollama call (Plan 03-02)
- Plan 03-02: `ChunkRef.source` opaque to LLM — only `id` + `text` rendered into the prompt. Filename/offset metadata stays available for Phase 6 UI citation chips but never enters Ollama (regression-tested via `test_build_user_message_does_not_render_source_metadata`, asserts neither 'secret.md' nor keys 'filename'/'offset' appear in rendered output). Prevents host-filesystem leak surface (Plan 03-02)
- Plan 03-02: Locked-content modules use per-file ruff ignores (`prompts.py = ["E501", "RUF001"]` + `tests/llm/test_prompts.py = ["S101", "RUF001"]`) — Hebrew lines from RESEARCH §5.2/§5.4 cannot be wrapped without altering byte content; RUF001 ambiguous-glyph warnings are noise on a Hebrew prompt module. Pattern: when a module's strings are byte-exact research artefacts, document the lock via `tool.ruff.lint.per-file-ignores` with comment naming lock source. Future Phase 7 prompt-tuning is the only owner allowed to modify these strings (Plan 03-02)
- Plan 03-02: LLM-03 grounding contract locked structurally at the prompt level — grep gate `grep -F 'אין לי מספיק מידע' backend/src/receptra/llm/prompts.py` returns 3 hits (SYSTEM_PROMPT_HE rule #2 + SYSTEM_PROMPT_EN rule #2 + FEW_SHOTS_HE example #2). Few-shot assistant entries are themselves byte-exact valid SuggestionResponse JSON, proven via `SuggestionResponse.model_validate_json(FEW_SHOTS_HE[i]['content'])` — pattern: prompt fixtures should self-validate through the production schema to prevent silent prompt drift (Plan 03-02)
- Plan 03-02: 47 tests across schema (19) + prompts (28); full backend suite 112 pass / 3 skip (live opt-in); ruff + mypy strict clean across 9 source files. TDD discipline followed: schema RED `dc4d7ec` → schema GREEN `81157e3`; prompts RED `669c8db` → prompts GREEN `49083f7`. LLM-03 + LLM-04 contracts published; Plans 03-03 (client) + 03-04 (engine) + 03-05 (metrics) + 03-06 (CLI harness) inherit `receptra.llm` surface unchanged (Plan 03-02)
- Plan 03-03: `receptra.llm.client` publishes the LLM-01 client layer with three pure functions: `get_async_client(host=None, timeout_s=None)` httpx.Timeout-bounded factory wrapping `ollama.AsyncClient` (T-03-03-01); `select_model(client)` startup probe — primary `dictalm3` → fallback `qwen2.5:7b` → `OllamaModelMissingError` with recovery hint; emits ONE structured `event="llm.model_selection"` loguru line (INFO on primary / WARN on fallback) carrying tags only (T-03-03-04); accepts BOTH ollama-python 0.6.x ListResponse-with-.models-attr shape AND old dict `{'models': [...]}` shape; `retry_with_strict_json(client, model, base_messages)` ONE-shot bounded retry with `stream=False` + `format='json'` (loose) + Hebrew strict-JSON suffix appended to messages[0] (system) only via defensive copy (T-03-03-06); returns raw completion string or `None` on httpx errors / empty / broad exception (engine maps None → canonical refusal — T-03-03-03 closed loop). Plain `Exception` subclasses `OllamaUnreachableError` + `OllamaModelMissingError` (Plan 03-03)
- Plan 03-03: STT-isolation regression at module boundary lands at `tests/llm/test_client.py::test_client_module_does_not_import_receptra_stt` — uses sys.modules save+restore around the import-under-test so the regression coexists with `test_schema.test_package_reexports_public_surface`'s class-identity assertion. The autouse conftest at `backend/tests/conftest.py` pre-imports `receptra.lifespan` (which transitively imports `receptra.stt.*`) for Whisper/VAD stub patching, so the regression compares the DELTA introduced by `import receptra.llm.client` rather than the absolute set. Pattern: when a module-import regression test must mutate `sys.modules`, save+restore in a `finally` block to keep cross-test class-identity stable. Plan 03-06 re-verifies STT-isolation at the harness boundary (T-03-03-05 defense-in-depth) (Plan 03-03)
- Plan 03-03: Tag-matching policy is repo-prefix-tolerant (`'dictalm3'` matches `'dictalm3:latest'`; `'qwen2.5:7b'` matches `'qwen2.5:14b'` — same repo, different tag). Strict-exact match would force every contributor to align tag suffixes; repo-prefix match accepts the de-facto `:latest` suffix that `make models dictalm` produces. Implemented via `_tag_present(target, available)` helper — exact match wins, otherwise split on `:` and compare repo prefix (Plan 03-03)
- Plan 03-03: Locked retry contract — ONE attempt only, no exponential backoff, no multi-attempt loop. `format='json'` (loose Ollama JSON mode), NOT `format=<schema>` mid-stream (RESEARCH §3.5 + §9 + upstream ollama-python issues #14440/#15260 — format=schema constraint does not strictly hold mid-stream). Broad `except Exception` is intentional (T-03-03-03): a malformed Ollama response or unexpected library error must NEVER propagate up the retry boundary; engine relies on the `None` return to deterministically fall back to canonical Hebrew refusal. WARN log captures error class + message for audit (Plan 03-03)
- Plan 03-03: 33 tests across factory (4) + helpers (8) + select_model (7) + retry_with_strict_json (8) + exceptions (2) + STT-isolation (1) + STT-isolation regression delta-aware. Full backend suite 145 pass / 3 skip (live opt-in only); ruff + mypy strict clean across 11 source files. TDD discipline: RED `fa9f8f6` → GREEN `59f61d2`. 3 deviations all auto-fixed (Rule 1 each: invalid test port `99999`; STT-isolation test breaking schema identity assertion via permanent sys.modules deletion; 7 ruff-cleanup issues — SIM110/RUF100/E702/E501/N814). LLM-01 client layer complete; Plan 03-04 (engine) consumes the surface without further wiring (Plan 03-03)
- Plan 03-04: `receptra.llm.engine.generate_suggestions(transcript, context_chunks, *, request_id=None, model=None, record_call=None) -> AsyncGenerator[SuggestionEvent, None]` is the LLM-02 internal interface Phase 4 RAG and Phase 5 hot-path consume directly. Five paths from a single function: hard short-circuit on empty context OR whitespace transcript → canonical refusal CompleteEvent BEFORE any Ollama call (LLM-03 strongest grounding defense, RESEARCH §5.5 layer 1, saves ~2s); happy path → TokenEvents + parsed CompleteEvent with measured TTFT (perf_counter monotonic); parse-retry → ONE-shot bounded retry via Plan 03-03 retry_with_strict_json on pydantic.ValidationError or json.JSONDecodeError (recovery yields CompleteEvent with status='parse_retry_ok'; exhaustion yields LlmErrorEvent + canonical refusal CompleteEvent); ollama_unreachable → OllamaUnreachableError or httpx.ConnectError yields LlmErrorEvent only; timeout → httpx.ReadTimeout yields LlmErrorEvent. DoS bounds from Plan 03-02 propagate: ValueError from build_user_message caught and yielded as LlmErrorEvent(code='no_context'). Streaming chat call deliberately omits format=<schema> kwarg (Pitfall A regression-tested via captured kwargs assertion); only retry uses format='json' loose with stream=False. OllamaModelMissingError collapses onto wire-level ollama_unreachable code with model_missing: prefix in detail — keeps LlmErrorEvent.code Literal narrow at 4 values; LlmCallTrace.status carries granular 7-value audit status for Phase 7. LlmCallTrace frozen dataclass exposes per-call structural data via record_call callback hook (default no-op; Plan 03-05 wires log_llm_call+insert_llm_call). Engine NEVER calls loguru/sqlite3 directly — keeps engine tests offline+writable-DB-free. record_call invocation wrapped in contextlib.suppress(Exception) in finally — defense in depth. _strip_markdown_fences pure helper handles ```json/```/triple-backticks; idempotent; preserves mid-body backticks; applied BEFORE pydantic parse on both first stream + retry response. _CANONICAL_REFUSAL Suggestion(text='אין לי מספיק מידע', confidence=0.0, citation_ids=[]) byte-exact from RESEARCH §5.5/§5.4 (Plan 03-04)
- Plan 03-04: 24 mocked-engine tests + 2 opt-in @pytest.mark.live tests (DictaLM 3.0 round-trip + grounding-refusal contract). Full backend suite 169 pass / 5 skip; ruff + mypy strict clean across 44 source files. TDD discipline: RED `ba3ec72` → GREEN `6ba0af1`; live test `26fd622` (Plan 03-04)
- Plan 03-04: New project pattern — module-reference monkeypatching. When sibling tests mutate sys.modules (e.g. tests/llm/test_client.py's STT-isolation save+restore), use `monkeypatch.setattr(module_obj, 'attr_name', new)` two-arg form instead of string path `'pkg.module.attr_name'`. Bypasses pytest derive_importpath's getattr-walk on the package object whose attributes may be wiped during the save+restore dance. Discovered via the standard "isolated tests pass + full suite fails" diagnostic; root-caused by tracing pytest derive_importpath against test_client.py's save+restore code. Pattern is reusable for any future Phase 3+ test files monkeypatching `receptra.llm.engine.X` (Plan 03-04 — Rule 1 deviation, auto-fixed in GREEN)
- Plan 03-05: `receptra.llm.metrics` publishes `LlmCallMetrics` (frozen dataclass with `ttft_ms`/`total_ms` derived `@property` over monotonic timestamps — NOT stored fields, mirrors `UtteranceMetrics.stt_latency_ms` from Plan 02-06; `ttft_ms` returns `-1` when `t_first_token is None`, otherwise clamped to `>= 0`). `from_trace(trace, request_id_override=None)` converts engine `LlmCallTrace` to `LlmCallMetrics`; computes `transcript_hash = sha256(transcript.encode("utf-8")).hexdigest()[:16]` (RESEARCH §6.4 PII boundary, 8 bytes prefix, 2^64 collision space — sufficient for cross-call correlation in Phase 7 audit; NOT cryptographic identity). `log_llm_call(m)` emits ONE structured loguru JSON line via `logger.bind(event="llm.call").info(payload)` — default OMITS `transcript` body (PII redaction); opt-in via `settings.llm_log_text_redaction_disabled=True` (mirrors `stt_log_text_redaction_disabled` byte-for-byte). `build_record_call(audit_path)` factory eager-inits the audit table at hook-construction time (T-02-06-06 fresh-checkout pattern); returned callable wraps `log_llm_call` and `insert_llm_call` in TWO independent `contextlib.suppress(Exception)` blocks so log failure does not skip insert and vice versa, neither propagates to engine generator (Plan 03-05)
- Plan 03-05: `receptra.llm.audit` is stdlib `sqlite3` only (no SQLAlchemy / aiosqlite — RESEARCH §Recommended Dependencies "Intentionally NOT added"). `init_llm_audit_table(path)` is idempotent (`CREATE TABLE IF NOT EXISTS llm_calls` + 2 `CREATE INDEX IF NOT EXISTS` for `idx_llm_calls_ts` + `idx_llm_calls_status`); creates parent dirs lazily; RESEARCH §6.4 schema verbatim (13 columns: id auto, request_id, transcript_hash, model, n_chunks, ttft_ms, total_ms, eval_count nullable, prompt_eval_count nullable, suggestions_count, grounded INTEGER 0/1, status, ts default `strftime('%Y-%m-%dT%H:%M:%fZ', 'now')`). `insert_llm_call(path, m)` opens per-call connection inside `with sqlite3.connect(...)` (atomic commits, zero thread-safety hazard); `grounded` mapped to `1 if m.grounded else 0`; deliberately does NOT lazy-init so insert-before-init surfaces as `sqlite3.OperationalError`. Co-exists cleanly with Phase 2's `stt_utterances` table on the same `data/audit.sqlite` file (regression-guarded via `test_coexistence_with_stt_utterances_table`). Phase 5 INT-05 may extend via `ALTER TABLE ADD COLUMN` (CREATE IF NOT EXISTS is forward-compatible) (Plan 03-05)
- Plan 03-05: 34 tests across metrics (17) + audit (17). Full backend suite 203 pass / 5 skip; ruff + mypy strict clean across 48 source files. TDD discipline: Task 1 RED `9894bc6` → Task 1 GREEN `a736d11`; Task 2 `61065e4`. 3 deviations all auto-fixed: (1) Rule 1 — test float-precision flake from non-representable 0.1-class floats (`int((10.6 - 10.0) * 1000)` truncates 599.999... to 599 not 600); fix switches test fixtures to power-of-2 fractions (10.0625, 10.5) so float arithmetic is exact, engine math unchanged; (2) Rule 1 — cross-test sys.modules contamination from `tests/llm/test_client.py` STT-isolation save+restore caused string-path `monkeypatch.setattr("receptra.llm.metrics.X", ...)` to fail under full-suite alphabetical ordering (same pattern Plan 03-04 hit). Fix extends Plan 03-04 module-reference lock to settings: import `receptra.config.settings` directly and patch attributes there (avoids mypy `attr-defined` error against metrics module `__all__` because `settings` is re-exported, not in `__all__`); (3) Rule 3 — ruff SIM105/RUF100/C408 cleanup (try/except→`contextlib.suppress`, `dict()`→`{}`). LLM-05 satisfied; engine surface unchanged (Plan 03-05)
- Plan 03-05: New project pattern extension — when patching settings used inside a module, prefer `from receptra.config import settings as receptra_settings; monkeypatch.setattr(receptra_settings, "attr", new)` over `monkeypatch.setattr(target_module, "settings", ...)`. Settings is a singleton imported by-reference into many modules; patching at its canonical home covers all callers AND keeps mypy happy when `__all__` does not list it. Pattern is reusable for any future test patching `settings.*` fields (Plan 03-05)

### Open Todos

- Execute Phase 1 research spike questions (see `research/STACK.md` §"Open questions")

### Blockers

(none)

### Skills in play (`skills-il`)

- `hebrew-nlp-toolkit` → Phase 4 (RAG chunking)
- `hebrew-document-generator` → Phase 4 (seed KB)
- `hebrew-tailwind-preset`, `hebrew-rtl-best-practices`, `hebrew-i18n`, `israeli-accessibility-compliance` → Phase 6
- `hebrew-content-writer`, `israeli-accessibility-compliance` → Phase 7

## Session Continuity

- **Last agent:** executor
- **Last action:** Completed Plan 03-05 (Wave-3 sibling — metrics + audit; LLM-05 satisfied). Two-task delivery — Task 1 RED `9894bc6` → Task 1 GREEN `a736d11` (metrics.py + audit.py implementation; audit.py landed alongside Task 1 GREEN because metrics.py imports `init_llm_audit_table`/`insert_llm_call` at module level); Task 2 `61065e4` (audit roundtrip + co-existence tests). `receptra.llm.metrics` publishes `LlmCallMetrics` (frozen dataclass with derived `ttft_ms`/`total_ms` `@property` over monotonic timestamps; -1 sentinel for no-token, clamp >= 0 against drift) + `from_trace(trace)` converter (computes `transcript_hash = sha256(transcript)[:16]` per RESEARCH §6.4 PII boundary) + `log_llm_call(m)` loguru JSON sink (event='llm.call' with default-on PII redaction; opt-in via `settings.llm_log_text_redaction_disabled`, mirrors `stt_log_text_redaction_disabled` byte-for-byte) + `build_record_call(audit_path)` factory (eager init_llm_audit_table at hook-construction; two independent `contextlib.suppress(Exception)` blocks so log/insert failures cannot crash engine generator). `receptra.llm.audit` uses stdlib sqlite3 only — `init_llm_audit_table(path)` idempotent CREATE TABLE IF NOT EXISTS `llm_calls` + 2 CREATE INDEX IF NOT EXISTS (idx_ts + idx_status); RESEARCH §6.4 schema verbatim with `grounded` INTEGER 0/1 + `ts` strftime UTC default; per-call connection inside `with sqlite3.connect(...)`; co-exists cleanly with Phase 2's `stt_utterances` on same `data/audit.sqlite` (regression-guarded). Verification proof: default loguru capture shows Hebrew transcript body `שלום עולם` ABSENT, `transcript_hash` PRESENT; `tables: ['llm_calls', 'sqlite_sequence', 'stt_utterances']` after both inits. 34 tests (17 metrics + 17 audit). Full backend suite 203 pass / 5 skip; ruff + mypy strict clean across 48 source files. 3 deviations all auto-fixed: (1) Rule 1 — test float-precision flake from non-representable 0.1-class floats; fix switches test fixtures to power-of-2 fractions (10.0625, 10.5) for exact arithmetic; (2) Rule 1 — cross-test sys.modules contamination caused string-path monkeypatch.setattr to fail under full-suite ordering; fix extends Plan 03-04 module-reference lock to settings (import `receptra.config.settings` directly and patch attributes there to avoid mypy attr-defined error against metrics module `__all__`); (3) Rule 3 — ruff SIM105/RUF100/C408 cleanup. Engine surface unchanged. LLM-05 satisfied.
- **Next action:** Plan 03-06 (Wave-3 sibling — CLI harness). Consumes `build_record_call(settings.audit_db_path)` directly so CLI invocations land alongside Phase 5 hot-path calls in the same audit DB. Re-verifies STT-isolation at the integration boundary (T-03-03-05). Documents `transcript_hash` SHA-256 prefix collision space (~2^64) in `docs/llm.md`. After 03-06: Phase 3 Hebrew Suggestion LLM 6/6 plans complete, milestone progress 18/18 plans (100%). Independent follow-ups still tracked: (1) Plan 02-05 numeric WER + 02-06 latency baseline on reference M2 hardware (HF auth + CV 25.0 license + Apple Silicon — separate from Phase 3); (2) Plan 03-01 live ChatML grep gate run on the first Mac with `dictalm3` registered; (3) Plan 03-04 grounding-refusal contract live test (`test_grounding_refusal_on_irrelevant_context_live`) as the v1 Phase 3 quality bar — first Mac contributor flips RECEPTRA_LLM_LIVE_TEST=1 and runs.
- **Last updated:** 2026-04-26

**Planned Phase:** 1 (Foundation) — 6 plans — 2026-04-23T19:12:18.810Z
**Plan 01-01 complete:** 2026-04-23T22:05:13Z — commits 1ba63fc, 3351d81, 7d45601
**Plan 01-02 complete:** 2026-04-23 — commits 530b3bc, 8578d8e, 3bc6df2
**Plan 01-03 complete:** 2026-04-24 — commits 64bcf99, 77b0d8f
**Plan 01-04 complete:** 2026-04-24 — commits 3ab2138, 3e5af29, 11c06bc
**Plan 01-05 complete:** 2026-04-24 — commits dc26343, 50cefeb
**Plan 01-06 complete:** 2026-04-24 — commits c44fc4d, 7a6b828
**Phase 1 Foundation COMPLETE:** 2026-04-24 — 6/6 plans, 6/6 FND-* requirements
**Plan 02-01 complete:** 2026-04-24 — commits 3928bdd, 6411ab5
**Plan 02-02 complete:** 2026-04-25 — commits 9584947, d95f693, c7f79c2, 2dde0a9 (TDD RED+GREEN x 2 tasks; STT-01 satisfied)
**Plan 02-03 complete:** 2026-04-25 — commits b1633cf, 801d658 (TDD RED+GREEN x 1 task; STT-02 satisfied — per-connection StreamingVad wrapper)
**Plan 02-04 complete:** 2026-04-25 — commits bc968b1, b99aa9b, 714703e, 591f0c3 (TDD RED+GREEN x 2 tasks; STT-03 + STT-04 satisfied — /ws/stt WebSocket endpoint with VAD-gated transcribe loop, pydantic event schema, asyncio.to_thread non-blocking)
**Plan 02-05 complete:** 2026-04-25 — commits 9ec96b2, 4df048f, 700c798 (TDD RED+GREEN for normalise_hebrew + compute_wer; tooling commit for fetch+CLI+regression+docs; STT-05 structurally satisfied — Hebrew WER eval harness with airgap fallback, BASELINE_WER/CER follow-up scheduled for Plan 02-06)
**Plan 02-06 complete:** 2026-04-25 — commits 24b836c, 58a6e7a, d5d007e (TDD RED+GREEN for metrics+audit; integration commit wiring pipeline + chaos test + docker volume + docs/stt.md; STT-06 satisfied — per-utterance latency instrumentation with PII-redacted loguru JSON sink + SQLite audit stub + RESEARCH §Validation Chaos disconnect-mid-utterance regression guard)
**Phase 2 Hebrew Streaming STT COMPLETE:** 2026-04-25 — 6/6 plans, 6/6 STT-* requirements (numeric WER + latency baseline on reference M2 hardware deferred as a contributor follow-up; harness + instrumentation are live)
**Plan 03-01 complete:** 2026-04-25 — commits c54064b, 5735da0 (Wave-0 dep lock — `ollama>=0.6.1,<1` (MIT) pinned + Settings extended with 9 LLM fields + .env.example RECEPTRA_LLM_* keys + tests/llm scaffold with `@pytest.mark.live` marker + Ollama AsyncClient.list smoke + DictaLM 3.0 ChatML grep gate; LLM-01 plumbing landed; live tests self-skipped on this executor — first Mac contributor with `dictalm3` registered runs the live gates on env-var flip)
**Plan 03-02 complete:** 2026-04-26 — commits dc4d7ec, 81157e3, 669c8db, 49083f7 (TDD RED+GREEN x 2 tasks; LLM-03 + LLM-04 satisfied — `receptra.llm` package skeleton with `Suggestion`/`SuggestionResponse` + discriminated `SuggestionEvent` union mirroring `stt.events.SttEvent` + `ChunkRef` frozen dataclass + `SYSTEM_PROMPT_HE` (RESEARCH §5.2 verbatim, sha256 5726ca37a5ea082fee7b4b1b0dfe38c797d587a02f60ffea5324c9d62b341e0f) + `SYSTEM_PROMPT_EN` placeholder + `FEW_SHOTS_HE` 4-turn list + `build_user_message` DoS-bounded + `build_messages` 6-element ChatML builder; 47 tests, full backend suite 112 pass / 3 skip, ruff + mypy strict clean; 1 Rule-3 deviation — per-file ruff ignores added for Hebrew locked-content files)
**Plan 03-03 complete:** 2026-04-26 — commits fa9f8f6, 59f61d2 (TDD RED+GREEN x 1 task; LLM-01 client layer satisfied — `receptra.llm.client` with `get_async_client(host=None, timeout_s=None)` httpx.Timeout-bounded AsyncClient factory + `select_model(client)` primary→fallback→typed-error probe with structured loguru observability + `retry_with_strict_json(client, model, base_messages)` ONE-shot bounded retry (stream=False + format='json' + Hebrew strict-JSON suffix on system msg only via defensive copy) + `OllamaUnreachableError`/`OllamaModelMissingError` plain Exception subclasses; 33 tests, full backend suite 145 pass / 3 skip, ruff + mypy strict clean across 11 source files; STT-isolation regression at module boundary with sys.modules save+restore pattern; 3 Rule-1 deviations — invalid test port + sys.modules cross-test contamination + 7 ruff cleanup issues, all auto-fixed inside the GREEN commit)
**Plan 03-04 complete:** 2026-04-26 — commits ba3ec72, 6ba0af1, 26fd622 (TDD RED+GREEN x 1 task + opt-in live tests; LLM-02 + LLM-03 + LLM-04 satisfied — `receptra.llm.engine.generate_suggestions(transcript, context_chunks, *, request_id=None, model=None, record_call=None) -> AsyncGenerator[SuggestionEvent, None]` with five paths from a single function: hard short-circuit on empty context OR whitespace transcript → canonical refusal CompleteEvent BEFORE any Ollama call; happy path → TokenEvents + parsed CompleteEvent with measured TTFT; parse-retry → ONE-shot bounded retry via Plan 03-03 helper; ollama_unreachable + timeout paths yield LlmErrorEvent only. DoS bounds from Plan 03-02 propagate via ValueError → LlmErrorEvent(code='no_context'). Streaming chat call deliberately omits format=<schema> kwarg (Pitfall A regression-tested); only retry uses format='json' loose with stream=False. OllamaModelMissingError collapses onto wire-level ollama_unreachable code with model_missing: prefix in detail. LlmCallTrace frozen dataclass exposes per-call structural data via record_call callback hook (default no-op; Plan 03-05 wires log_llm_call+insert_llm_call). _strip_markdown_fences pure helper handles ```json/```/triple-backticks; idempotent. _CANONICAL_REFUSAL Suggestion(text='אין לי מספיק מידע') byte-exact from RESEARCH §5.5/§5.4. 24 mocked-engine tests + 2 opt-in @pytest.mark.live tests (DictaLM 3.0 round-trip + grounding-refusal contract). Full backend suite 169 pass / 5 skip, ruff + mypy strict clean across 44 source files. 2 deviations auto-fixed: Rule 1 cross-test sys.modules pollution (string-path monkeypatch.setattr fails under full-suite alphabetical ordering when test_client mutates sys.modules; fix establishes new project pattern: module-reference monkeypatch.setattr two-arg form) + Rule 3 ruff RUF022/I001 import-sort cleanup; both folded into GREEN commit)
**Plan 03-05 complete:** 2026-04-26 — commits 9894bc6, a736d11, 61065e4 (TDD RED+GREEN x 2 tasks; LLM-05 satisfied — `receptra.llm.metrics` publishes `LlmCallMetrics` frozen dataclass with derived `ttft_ms`/`total_ms` `@property` over monotonic timestamps (-1 sentinel for no-token + clamp >= 0 against drift, mirrors `UtteranceMetrics.stt_latency_ms`) + `from_trace(trace)` converter (computes `transcript_hash = sha256(transcript)[:16]` per RESEARCH §6.4 PII boundary) + `log_llm_call(m)` loguru JSON sink emitting `event="llm.call"` with default-on PII redaction (transcript body OMITTED unless `settings.llm_log_text_redaction_disabled=True`, mirrors `stt_log_text_redaction_disabled` byte-for-byte) + `build_record_call(audit_path)` factory (eager `init_llm_audit_table` at hook construction; two independent `contextlib.suppress(Exception)` blocks so log/insert failures cannot crash engine generator). `receptra.llm.audit` uses stdlib sqlite3 only — `init_llm_audit_table(path)` idempotent CREATE TABLE IF NOT EXISTS `llm_calls` + 2 CREATE INDEX IF NOT EXISTS (idx_ts + idx_status); RESEARCH §6.4 schema verbatim with `grounded` INTEGER 0/1 + `ts` strftime UTC default; per-call connection inside `with sqlite3.connect(...)`. Co-exists cleanly with Phase 2's `stt_utterances` on same `data/audit.sqlite` (regression-guarded). 34 tests (17 metrics + 17 audit). Full backend suite 203 pass / 5 skip; ruff + mypy strict clean across 48 source files. 3 deviations all auto-fixed: Rule 1 test float-precision flake (power-of-2 fractions fix), Rule 1 cross-test sys.modules contamination (extends Plan 03-04 module-reference lock to settings via `receptra.config.settings`), Rule 3 ruff SIM105/RUF100/C408 cleanup. Engine surface unchanged.)
