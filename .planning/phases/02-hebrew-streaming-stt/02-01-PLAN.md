---
phase: 02-hebrew-streaming-stt
plan: 02-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - scripts/spike_stt_latency.py
  - scripts/check_licenses.sh
  - .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md
autonomous: true
requirements: []
tags: [stt, spike, dependencies, apple-silicon, faster-whisper, silero-vad]

must_haves:
  truths:
    - "Phase 2 runtime + dev dependencies are declared in backend/pyproject.toml and locked in backend/uv.lock"
    - "uv sync succeeds on Python 3.12 arm64 with no wheel fallback"
    - "License allowlist in scripts/check_licenses.sh covers torch + onnxruntime license strings (OPEN-4)"
    - "A measured (or explicitly UNMEASURED placeholder) first-transcribe and steady-state wall-clock baseline is recorded in 02-01-SPIKE-RESULTS.md"
    - "The recorded baseline locks the default settings.stt_partial_interval_ms value that downstream plans 02-04 / 02-06 will consume"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Pinned Phase 2 deps (faster-whisper>=1.2.1,<2; silero-vad>=6.2.1,<7; numpy>=2.0,<3; loguru>=0.7.3,<1) + dev deps (jiwer>=4.0,<5; soundfile>=0.13)"
      contains: "faster-whisper"
    - path: "backend/uv.lock"
      provides: "Locked transitive resolution including ctranslate2 4.7.x arm64 wheel + torch 2.11 macOS arm64 wheel"
      contains: "ctranslate2"
    - path: "scripts/spike_stt_latency.py"
      provides: "Standalone script that loads whisper-turbo-ct2, warms up, transcribes a synthetic 2s buffer, prints p50/p95 wall-clock ms"
      min_lines: 40
    - path: ".planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md"
      provides: "Wave 0 spike measurements + partial_interval_decision that downstream plans consume"
      contains: "partial_interval_decision"
    - path: "scripts/check_licenses.sh"
      provides: "License allowlist that accepts torch's BSD-style + onnxruntime's MIT license strings as emitted by pip-licenses"
  key_links:
    - from: "backend/pyproject.toml"
      to: "backend/uv.lock"
      via: "uv lock"
      pattern: "faster-whisper"
    - from: "scripts/spike_stt_latency.py"
      to: "$RECEPTRA_MODEL_DIR/whisper-turbo-ct2"
      via: "faster_whisper.WhisperModel(device='cpu', compute_type='int8', cpu_threads=4)"
      pattern: "WhisperModel"
    - from: "scripts/check_licenses.sh"
      to: "backend/uv.lock transitive torch + onnxruntime"
      via: "pip-licenses --from=mixed --allow-only"
      pattern: "BSD|MIT|Apache"
---

<objective>
Lock the Phase 2 dependency surface into backend/pyproject.toml and uv.lock, verify the license allowlist accepts the new transitive deps (torch + onnxruntime — OPEN-4), and run a Wave-0 latency spike on the real ivrit-ai/whisper-large-v3-turbo-ct2 model to lock the partial-re-transcribe cadence (OPEN-2) BEFORE any WebSocket plumbing is written.

Purpose: RESEARCH §2, §Assumptions Log A2/A7, §Open Decisions OPEN-2/OPEN-4, and §Validation Architecture Wave-0 SPIKE all require a measured latency number and a verified license gate before Plans 02-02..02-06 commit. Getting this wrong upstream wastes all downstream work.

Output: An amended `backend/pyproject.toml` + regenerated `backend/uv.lock` with Phase 2 deps; `scripts/spike_stt_latency.py` reproducible measurement tool; `02-01-SPIKE-RESULTS.md` containing measured numbers and the locked `stt_partial_interval_ms` value; `scripts/check_licenses.sh` passing against the new lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-hebrew-streaming-stt/02-RESEARCH.md
@backend/pyproject.toml
@scripts/check_licenses.sh

<interfaces>
Phase 1 contracts this plan consumes:

From Phase 1 Plan 01-05 (scripts/download_models.sh):
- `hf download ivrit-ai/whisper-large-v3-turbo-ct2 --local-dir ${MODEL_DIR}/whisper-turbo-ct2`
- MODEL_DIR default: `${HOME}/.receptra/models` (from .env.example)

From Phase 1 Plan 01-05 (scripts/check_licenses.sh):
- Uses `uv run pip-licenses --from=mixed --allow-only="{SEMI-COLON-SEPARATED-LIST}"` for backend
- Allowlist is the single source of truth; extend via this script only

From backend/pyproject.toml (current state):
- requires-python = ">=3.12"
- Existing runtime: fastapi, uvicorn[standard], pydantic>=2.9, pydantic-settings>=2.6, python-multipart>=0.0.20
- Existing dev: ruff>=0.7, mypy>=1.13, pip-licenses>=5.0, pytest>=8.3, pytest-asyncio>=0.24, httpx>=0.27

From backend/src/receptra/config.py:
- `settings.model_dir: str = "/models"` (container-side default). The spike script reads `RECEPTRA_MODEL_DIR` env var, falling back to `${HOME}/.receptra/models` for local invocation — do NOT use the in-container `/models` default when running from the host.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Phase 2 deps + regenerate uv.lock + verify license allowlist</name>
  <files>backend/pyproject.toml, backend/uv.lock, scripts/check_licenses.sh</files>
  <action>
Edit `backend/pyproject.toml` to add Phase 2 deps per RESEARCH §Recommended Dependencies.

Under `[project].dependencies` append:
- `"faster-whisper>=1.2.1,<2"`
- `"silero-vad>=6.2.1,<7"`
- `"numpy>=2.0,<3"`
- `"loguru>=0.7.3,<1"`

Under `[dependency-groups].dev` append:
- `"jiwer>=4.0,<5"`
- `"soundfile>=0.13"`

From `backend/` run `uv lock` to regenerate `uv.lock`. The lock MUST include (transitively): ctranslate2 4.7.x, torch 2.11.x, onnxruntime, tokenizers, av.

Then verify the Phase 1 license gate still passes. From `backend/`:
```
uv sync --all-groups
uv run pip-licenses --from=mixed --format=plain-vertical | grep -iE "^(Name|License):" | head -100
```

Capture the exact License strings reported for `torch` and `onnxruntime`. Read the existing allowlist from `scripts/check_licenses.sh` (find the `--allow-only="..."` string).

Decision tree (per RESEARCH OPEN-4):
- If both torch's License string AND onnxruntime's License string are already present verbatim in the allowlist → NO edit needed. Record "OPEN-4 pre-existing coverage" in the commit message.
- If either is missing → append the exact license name(s) (separated by `;`) to the allowlist string in `scripts/check_licenses.sh`. Do NOT substitute shorter SPDX tags for long-form names — match exactly what pip-licenses emits.

After the edit (or no edit), re-run from repo root:
```
bash scripts/check_licenses.sh
```

This MUST exit 0. Do NOT use `--ignore-packages`, `|| true`, or any silencing flag. Extend the allowlist verbatim or fail loud.
  </action>
  <verify>
    <automated>cd /Users/stavnsananes/Applications/receptra/.claude/worktrees/practical-franklin-071ba2/backend &amp;&amp; uv sync --all-groups &amp;&amp; uv run python -c "import faster_whisper, silero_vad, jiwer, loguru, soundfile; print('imports ok')" &amp;&amp; cd .. &amp;&amp; bash scripts/check_licenses.sh</automated>
  </verify>
  <done>
- `backend/pyproject.toml` lists all 4 new runtime deps + 2 new dev deps with the exact version specifiers.
- `backend/uv.lock` regenerated; all of faster-whisper, silero-vad, ctranslate2, torch, onnxruntime, jiwer, loguru, soundfile resolved.
- `uv run python -c "import faster_whisper, silero_vad, jiwer, loguru, soundfile"` succeeds from `backend/`.
- `bash scripts/check_licenses.sh` exits 0.
- No `|| true`, `--ignore-packages`, or silencing flags introduced in `scripts/check_licenses.sh`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write + run Wave-0 latency spike; lock OPEN-2 partial cadence</name>
  <files>scripts/spike_stt_latency.py, .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md</files>
  <action>
Create `scripts/spike_stt_latency.py` — a standalone reproducible spike per RESEARCH §3 (singleton load), §7 (transcribe params), §11 (monotonic clocks).

Requirements:

1. Resolve model path: `Path(os.environ.get("RECEPTRA_MODEL_DIR", str(Path.home() / ".receptra" / "models"))) / "whisper-turbo-ct2"`. If the path does not exist, exit 1 with an actionable error pointing to `make models` (Phase 1 Plan 01-05).

2. Construct the model with parameters LOCKED per RESEARCH §2/§3:
   ```python
   WhisperModel(
       str(model_path),
       device="cpu",
       compute_type="int8",
       cpu_threads=4,
       num_workers=1,
   )
   ```
   Do NOT parametrize these.

3. Time the load separately: `t0 = time.monotonic(); model = WhisperModel(...); model_load_ms = (time.monotonic() - t0) * 1000`.

4. Run one warmup transcribe on `np.zeros(16000, dtype=np.float32)` (1 second of silence) with the LOCKED transcribe kwargs per RESEARCH §7:
   ```python
   language="he"
   task="transcribe"
   beam_size=1
   best_of=1
   temperature=0.0
   condition_on_previous_text=False
   vad_filter=False
   without_timestamps=True
   ```
   Record `warmup_transcribe_ms`.

5. Generate a SYNTHETIC 2-second buffer: 440 Hz sine wave at 16 kHz, amplitude 0.3, dtype float32. Intentionally synthetic — real Hebrew fixtures land in Plan 02-05.

6. Transcribe the 2s buffer N=10 times (argparse `--iters`, default 10). Time each with `time.monotonic()`. Compute p50 (statistics.median) and p95 (sorted[int(0.95 * (N-1))]).

7. Print a JSON summary to stdout AND write `.planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md` with:
   - `model_load_ms`
   - `warmup_transcribe_ms`
   - `steady_state_p50_ms`
   - `steady_state_p95_ms`
   - `iters`
   - `host_info` (using `platform.machine()`, `platform.processor()`, `platform.mac_ver()[0]`, `os.cpu_count()` — do NOT add psutil)
   - `partial_interval_decision` (700, 1000, or "BLOCKED" per decision rule)
   - Exact reproduction command
   - One-paragraph interpretation

Use loguru for progress logging (exercises the dep). Use `time.monotonic()` exclusively. The 440 Hz sine is deterministic.

**Decision rule embedded in the script AND the results doc (contract that downstream plans consume):**
- `steady_state_p95_ms <= 700` → `partial_interval_decision = 700` (RESEARCH default; OPEN-2 Option A confirmed)
- `700 < steady_state_p95_ms <= 1000` → `partial_interval_decision = 1000` (RESEARCH §7 fallback)
- `steady_state_p95_ms > 1000` → `partial_interval_decision = "BLOCKED"`; flag as BLOCKER for Plan 02-04 in the results doc; recommend escalating to OPEN-3 (whisper.cpp + Core ML) or accepting partials >1s (violates STT-03)

After implementing, run from repo root:
```
cd backend && uv run python ../scripts/spike_stt_latency.py
```

**If the executor's environment does not have the model available** (e.g., `make models` has not been run), the script exits 1. In that case, still create `02-01-SPIKE-RESULTS.md` but label every numeric field as `UNMEASURED — contributor must run on M2 hardware before Plan 02-04 locks`. Include the reproduction command. Set `partial_interval_decision = 700` as a provisional default AND pin a follow-up note: "Plan 02-06 will re-run this spike on reference M2 hardware and bump to 1000 if p95 > 700ms."

CRITICAL:
- Do NOT add psutil or any new dep for this spike.
- Do NOT commit any audio file (the sine buffer is generated in memory).
- Do NOT use `time.time()` — monotonic only.
- Do NOT skip the warmup transcribe — RESEARCH Pitfall 7 explicitly warns first-transcribe is 2-3x slower than steady state.
  </action>
  <verify>
    <automated>cd /Users/stavnsananes/Applications/receptra/.claude/worktrees/practical-franklin-071ba2 &amp;&amp; python3 -c "
import ast, pathlib
src = pathlib.Path('scripts/spike_stt_latency.py').read_text()
tree = ast.parse(src)
imports = set()
for n in ast.walk(tree):
    if isinstance(n, ast.ImportFrom) and n.module:
        imports.add(n.module.split('.')[0])
    elif isinstance(n, ast.Import):
        for a in n.names:
            imports.add(a.name.split('.')[0])
for mod in ['faster_whisper', 'loguru', 'numpy']:
    assert mod in imports, f'missing import: {mod}'
for needle in ['compute_type', 'cpu_threads=4', 'language=', 'monotonic']:
    assert needle in src, f'missing contract term: {needle}'
assert 'int8' in src, 'int8 compute type not pinned'
assert 'condition_on_previous_text=False' in src, 'cond_on_prev_text=False not pinned'
assert 'partial_interval_decision' in src, 'decision rule missing'
print('spike script contract verified')
" &amp;&amp; test -f .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md &amp;&amp; grep -qE "(steady_state_p95_ms|UNMEASURED)" .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md &amp;&amp; grep -qE "partial_interval_decision" .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md</automated>
  </verify>
  <done>
- `scripts/spike_stt_latency.py` exists and passes the AST contract check above.
- `.planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md` exists with either measured numbers (if model available) or explicit `UNMEASURED` placeholders, AND a `partial_interval_decision` field (700, 1000, or BLOCKED).
- `partial_interval_decision` is the contract Plan 02-04 consumes; any BLOCKED outcome is escalated before Plan 02-04 executes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local developer filesystem → spike script | Spike reads model weights from `~/.receptra/models`; weights were verified by Phase 1 Plan 01-05 via `hf download` integrity. No untrusted input crosses. |
| pip-licenses (read-only over uv.lock) → license allowlist gate | License strings come from PyPI-hosted wheels resolved by uv; supply-chain risk mitigated by uv.lock pinning exact versions + hashes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `uv.lock` supply chain (arm64 wheel substitution for torch / onnxruntime / ctranslate2) | mitigate | `uv.lock` pins exact version + hash for every resolved package; CI re-verifies lock on every push (Phase 1 Plan 01-06 gate). No `--frozen=false` or bypass flags in this plan. |
| T-02-01-02 | Elevation of Privilege | License gate silenced to let a GPL/AGPL dep slip through | mitigate | Action explicitly prohibits `--ignore-packages`, `\|\| true`, or any pip-licenses silencing flag. Allowlist is extended verbatim with the string pip-licenses emits, preserving the gate's detection power. |
| T-02-01-03 | Information Disclosure | Spike results file accidentally leaks a Hebrew transcript to committed git history | accept | The 2s buffer is a synthetic 440 Hz sine — it cannot be decoded into any real speech. No Hebrew text is processed in the spike. |
| T-02-01-04 | Denial of Service | CI runs the spike every push, blocking the pipeline for 3-5s model load + transcribes | mitigate | The spike script is NOT wired into CI — it is a manual contributor tool run once to lock the partial cadence. Results are committed as a markdown artifact. |
</threat_model>

<verification>
Full plan verification (both tasks must pass):

1. `cd backend && uv sync --all-groups` → 0 exit, no wheel-build fallback
2. `cd backend && uv run python -c "import faster_whisper, silero_vad, jiwer, loguru, soundfile"` → 0 exit
3. `bash scripts/check_licenses.sh` → 0 exit
4. `python3 -c "import ast; ast.parse(open('scripts/spike_stt_latency.py').read())"` → 0 exit
5. `test -f .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md` → 0 exit
6. `grep -q partial_interval_decision .planning/phases/02-hebrew-streaming-stt/02-01-SPIKE-RESULTS.md` → 0 exit
</verification>

<success_criteria>
- Dependency lock regenerated with all 8 required packages (4 runtime + 2 dev + ctranslate2 + torch transitively) resolved.
- License gate green against the new lock.
- Spike script is deterministic (440 Hz sine, monotonic clocks, pinned transcribe kwargs).
- `02-01-SPIKE-RESULTS.md` carries a single authoritative `partial_interval_decision` value (700, 1000, or BLOCKED) that Plan 02-04 consumes without ambiguity.
- BLOCKED outcome triggers an explicit escalation path (documented in the results file) before Plan 02-04 can execute.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hebrew-streaming-stt/02-01-SUMMARY.md` capturing:
- Final dependency versions resolved in uv.lock (9 packages)
- License allowlist delta (if any)
- Measured / placeholder spike numbers
- Locked `partial_interval_decision` value
- Pointer to `02-01-SPIKE-RESULTS.md` as the durable contract for Plans 02-02..02-06
</output>
