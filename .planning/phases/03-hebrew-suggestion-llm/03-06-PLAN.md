---
phase: 03-hebrew-suggestion-llm
plan: 03-06
type: execute
wave: 5
depends_on: ["03-04", "03-05"]
files_modified:
  - scripts/eval_llm.py
  - fixtures/llm/policy_returns.json
  - fixtures/llm/policy_hours.json
  - fixtures/llm/empty_context.json
  - fixtures/llm/eval_set.jsonl
  - backend/tests/llm/test_harness_isolation.py
  - docs/llm.md
autonomous: true
requirements: [LLM-06]
tags: [llm, cli, harness, eval, fixtures, stt-isolation, docs, phase-3-close]

must_haves:
  truths:
    - "`scripts/eval_llm.py` is the Phase 3 user-facing entry point — runs `generate_suggestions` end-to-end without ANY `receptra.stt.*` imports (LLM-06 structural lock)"
    - "Single-shot mode: `--transcript T --context-file FILE` consumes a JSON file shaped `[{id,text,source?},...]`, prints TokenEvent deltas to stderr (live token feed), prints final CompleteEvent JSON to stdout, prints summary line `TTFT: N ms  TOTAL: N ms  MODEL: dictalm3  GROUNDED: true|false` to stderr"
    - "Eval-set mode: `--eval-set fixtures/llm/eval_set.jsonl [--out-jsonl OUT]` iterates over JSONL entries shaped `{id, transcript, context, expected: {grounded, refusal}}`, runs each through the engine, and prints per-line results plus a final aggregate (count, mean TTFT, p95 TTFT, refusal rate, grounded rate, parse-retry rate, parse-error rate)"
    - "CLI flags published: `--transcript`, `--context-file`, `--eval-set`, `--out-jsonl`, `--model`, `--ollama-host`, `--system-prompt-lang`, `--no-stream` (omits stderr token feed)"
    - "Exit codes: 0 = success, 1 = ollama unreachable, 2 = parse error rate above 5% threshold (eval-set mode only)"
    - "Three single-shot fixtures committed: `policy_returns.json` (1 chunk → grounded reply expected), `policy_hours.json` (1 chunk irrelevant to question → refusal expected), `empty_context.json` (0 chunks → short-circuit refusal)"
    - "`eval_set.jsonl` ships with 5 lines covering: grounded reply, irrelevant-context refusal, empty-context refusal, multi-chunk grounded reply, edge case (very short transcript). Phase 7 grows this to 20."
    - "`backend/tests/llm/test_harness_isolation.py` is the LLM-06 STT-isolation regression: imports `scripts.eval_llm` (or executes `python -c 'import sys; sys.path.insert(0, \"scripts\"); import eval_llm'`) and asserts `\"receptra.stt\" not in sys.modules` AND `\"faster_whisper\" not in sys.modules` AND `\"silero_vad\" not in sys.modules`"
    - "`docs/llm.md` (≥150 lines) ships parallel to `docs/stt.md`: covers internal interface contract, CLI usage, fixture format, audit log + PII warning, troubleshooting (Ollama unreachable, model missing, parse_error spike, ChatML auto-detection failure), known limitations, links to RESEARCH §6"
    - "CLI smoke regression: `uv run --project backend python scripts/eval_llm.py --transcript 'שלום' --context-file fixtures/llm/empty_context.json --no-stream` exits 0 + stdout contains `אין לי מספיק מידע` (proves end-to-end short-circuit path runs without Ollama dependency)"
    - "PHASE 3 COMPLETE: 6/6 LLM-* requirements delivered across plans 03-01..03-06"
  artifacts:
    - path: "scripts/eval_llm.py"
      provides: "CLI harness — argparse + asyncio.run + record_call wired to settings.audit_db_path"
      min_lines: 180
    - path: "fixtures/llm/policy_returns.json"
      provides: "Single-chunk grounded fixture (returns policy + matching question)"
    - path: "fixtures/llm/policy_hours.json"
      provides: "Single-chunk irrelevant fixture (returns policy + question about hours → refusal)"
    - path: "fixtures/llm/empty_context.json"
      provides: "Zero-chunk fixture — exercises Plan 03-04 short-circuit"
    - path: "fixtures/llm/eval_set.jsonl"
      provides: "5-line eval set; Phase 7 grows to 20"
    - path: "backend/tests/llm/test_harness_isolation.py"
      provides: "LLM-06 structural regression: harness import does not pull in receptra.stt / faster_whisper / silero_vad"
    - path: "docs/llm.md"
      provides: "Phase 3 user/contributor doc"
      min_lines: 150
  key_links:
    - from: "scripts/eval_llm.py"
      to: "receptra.llm.engine.generate_suggestions"
      via: "asyncio.run(harness_main(...))"
      pattern: "from receptra.llm.engine import generate_suggestions"
    - from: "scripts/eval_llm.py"
      to: "receptra.llm.metrics.build_record_call"
      via: "record_call=build_record_call(settings.audit_db_path)"
      pattern: "build_record_call"
    - from: "backend/tests/llm/test_harness_isolation.py"
      to: "scripts/eval_llm.py module import"
      via: "subprocess.run([python, '-c', 'import sys; ...; import eval_llm; assert ...'])"
      pattern: "receptra.stt"
    - from: "docs/llm.md"
      to: "RESEARCH §6"
      via: ".planning/phases/03-hebrew-suggestion-llm/03-RESEARCH.md"
      pattern: "RESEARCH"
---

<objective>
Close Phase 3. This plan delivers the user-facing CLI harness (LLM-06), the structural STT-isolation regression that proves the harness is independent of the STT pipeline, three committed JSON fixtures + a 5-line eval set, and the `docs/llm.md` user/contributor doc parallel to `docs/stt.md`.

Why a CLI is required: LLM-06 explicitly says "suggestion engine is testable via a CLI harness independent of the STT pipeline." Phase 7 prompt-tuning will run `scripts/eval_llm.py --eval-set` thousands of times against varying prompts; that requires (a) zero STT load (no Whisper weights, no VAD), (b) deterministic exit codes for CI gating, (c) JSONL output for prompt-eval scripts. The independence is structural — the harness imports only `receptra.llm.*` + `receptra.config`. A regression test asserts the import contract holds.

Output: `scripts/eval_llm.py` (~200 LOC), 4 fixture files, 1 isolation test, 1 user-facing doc (~180 lines). After this plan lands, all 6 LLM-* requirements are delivered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-hebrew-suggestion-llm/03-RESEARCH.md
@.planning/phases/02-hebrew-streaming-stt/02-05-PLAN.md
@scripts/eval_wer.py
@docs/stt.md

<interfaces>
Consumed from Plan 03-04 (Wave 3):
- `generate_suggestions(transcript, context_chunks, *, model=None, record_call=None)` — primary entry point
- `LlmCallTrace` (not directly consumed; passes through `record_call`)

Consumed from Plan 03-05 (Wave 4):
- `build_record_call(audit_path)` — wires loguru + SQLite audit to the engine

Consumed from Plan 03-02 (Wave 2):
- `ChunkRef` — JSON fixture rows are deserialized into `list[ChunkRef]`
- `Suggestion`, `CompleteEvent`, `TokenEvent`, `LlmErrorEvent`, `SuggestionEvent`

Consumed from Plan 03-01 (Wave 1):
- `settings.ollama_host`, `settings.audit_db_path`, `settings.llm_system_prompt_lang`, `settings.llm_model_tag`

Consumed from Phase 2 reference (NOT IMPORT):
- `scripts/eval_wer.py` is the styling template; same argparse + asyncio.run + JSONL output idiom.
- `docs/stt.md` is the doc template; mirror sections (Overview, Internal Interface Contract, CLI Usage, Audit Log + PII Warning, Known Limitations, Troubleshooting).

Published for Phase 4 RAG (future):
- The `[{id, text, source?}, ...]` fixture JSON shape is the contract Phase 4 retrieval will emit; `eval_set.jsonl` consumers read this shape.

Published for Phase 7 (future):
- `eval_set.jsonl` 5-line set is the seed; Phase 7 grows to 20 + adds qualitative review by a Hebrew speaker.
- `--out-jsonl` mode produces line-per-eval JSONL output (`{id, ttft_ms, total_ms, status, suggestions, grounded, refusal}`) for downstream prompt-eval scripts.

LOCKED CLI surface:

```
Usage:
  python scripts/eval_llm.py --transcript T --context-file FILE [options]
  python scripts/eval_llm.py --eval-set FILE [--out-jsonl OUT] [options]

Options:
  --model TAG                 Override settings.llm_model_tag (e.g. qwen2.5:7b)
  --ollama-host URL           Override settings.ollama_host
  --system-prompt-lang LANG   he (default) | en
  --no-stream                 Suppress stderr TokenEvent feed (single-shot only)
  --no-audit                  Skip wiring build_record_call (useful for CI smoke
                              that does not have ./data writable)
```

Exit codes:
  0 = success
  1 = ollama unreachable (any LlmErrorEvent with code='ollama_unreachable' or 'timeout')
  2 = parse error rate above 5% threshold (eval-set mode only; single-shot ignores)

Threshold rationale: 5% is the operational quality gate. If 1-in-20 calls fails to parse despite the bounded retry, the prompt or model is degraded — Phase 7 prompt-tuner attention warranted. RESEARCH §7.2.

Single-shot fixture JSON shape (`policy_returns.json` example):
```json
[
  {
    "id": "kb-policy-returns",
    "text": "מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.",
    "source": {"filename": "policies.md", "offset": "12"}
  }
]
```
Top-level array (NOT object). `source` is optional. Empty array `[]` is valid (exercises short-circuit).

Eval set JSONL line shape:
```json
{"id":"eval-001","transcript":"תוך כמה זמן אני יכול להחזיר מוצר?","context":[{"id":"kb-policy-returns","text":"..."}],"expected":{"grounded":true,"refusal":false}}
```
The `expected.refusal` field is the assertion bar: when true, `suggestions[0].text == "אין לי מספיק מידע"` is required for the row to be marked PASS. When false, any non-refusal grounded reply counts as PASS. Aggregate output reports `pass_rate`.

LOCKED isolation contract (LLM-06 structural):
- `scripts/eval_llm.py` imports ONLY: argparse, asyncio, json, os, pathlib, sys, typing (stdlib), receptra.config, receptra.llm.engine, receptra.llm.metrics, receptra.llm.schema (project)
- No transitive `receptra.stt.*` imports allowed.
- No `faster_whisper` / `silero_vad` / `torch` / `onnxruntime` imports allowed.
- The regression test runs the harness module in a SUBPROCESS via `python -c "import scripts.eval_llm; ..."` so module-state from other tests cannot pollute the assertion. Subprocess uses the project's venv (`uv run`).

`docs/llm.md` outline (parallel to `docs/stt.md`):
1. **Overview** — what Phase 3 ships, the internal interface boundary
2. **Internal Interface Contract** — `generate_suggestions(...)` signature + Event shapes (Suggestion, CompleteEvent, LlmErrorEvent codes Literal allowlist)
3. **CLI Usage** — `scripts/eval_llm.py` examples for both modes; fixture format
4. **Audit Log + PII Warning** — `event="llm.call"` JSON shape; `transcript_hash` is sha256[:16]; opt-in `RECEPTRA_LLM_LOG_TEXT_REDACTION_DISABLED=true` weakens PII boundary; "DO NOT attach audit DB to bug reports"
5. **Grounding Contract (LLM-03)** — short-circuit + canonical refusal `"אין לי מספיק מידע"` + bounded retry path
6. **Live Tests** — `RECEPTRA_LLM_LIVE_TEST=1 pytest tests/llm/test_engine_live.py` workflow on Mac + ChatML auto-detection grep gate
7. **Known Limitations** — DictaLM 3.0 is non-deterministic at temp=0; `format=schema` not used while streaming (Pitfall A); Hebrew system prompt only; prompt injection accepted-risk; logprobs deferred to Phase 7
8. **Troubleshooting** — Ollama unreachable / model missing / parse_error rate spike / TTFT > 1 s / ChatML auto-detect failure
9. **Cross-references** — link to RESEARCH §6 + Plan 03-01..03-06 SUMMARYs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures (3 single-shot JSON + 1 eval-set JSONL)</name>
  <files>fixtures/llm/policy_returns.json, fixtures/llm/policy_hours.json, fixtures/llm/empty_context.json, fixtures/llm/eval_set.jsonl</files>
  <action>
Create `fixtures/llm/policy_returns.json`:

```json
[
  {
    "id": "kb-policy-returns",
    "text": "מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.",
    "source": {"filename": "policies.md", "offset": "12"}
  }
]
```

Create `fixtures/llm/policy_hours.json` (chunk is the SAME returns policy; question — see eval_set — is about hours, expected refusal):

```json
[
  {
    "id": "kb-policy-returns",
    "text": "מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.",
    "source": {"filename": "policies.md", "offset": "12"}
  }
]
```

Create `fixtures/llm/empty_context.json`:

```json
[]
```

Create `fixtures/llm/eval_set.jsonl` — 5 lines, ONE per row, each is a complete JSON object on its own line (NO trailing newline after the last line is fine but acceptable):

Line 1 — grounded reply (returns policy + returns question):
```json
{"id":"eval-001","transcript":"תוך כמה זמן אני יכול להחזיר מוצר?","context":[{"id":"kb-policy-returns","text":"מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית."}],"expected":{"grounded":true,"refusal":false}}
```

Line 2 — irrelevant-context refusal (returns policy + hours question):
```json
{"id":"eval-002","transcript":"מה שעות הפעילות של החנות?","context":[{"id":"kb-policy-returns","text":"מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית."}],"expected":{"grounded":false,"refusal":true}}
```

Line 3 — empty-context refusal (no chunks):
```json
{"id":"eval-003","transcript":"איך אני יכול ליצור איתכם קשר?","context":[],"expected":{"grounded":false,"refusal":true}}
```

Line 4 — multi-chunk grounded reply (two policies + matching question):
```json
{"id":"eval-004","transcript":"מתי אני יכול לאסוף את ההזמנה?","context":[{"id":"kb-policy-returns","text":"מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית."},{"id":"kb-policy-pickup","text":"איסוף הזמנות: ניתן לאסוף הזמנות בסניף בימים א-ה בין 09:00 ל-18:00, ובימי שישי בין 09:00 ל-13:00."}],"expected":{"grounded":true,"refusal":false}}
```

Line 5 — edge case, very short transcript (single word question with answer in chunk):
```json
{"id":"eval-005","transcript":"החזרים?","context":[{"id":"kb-policy-returns","text":"מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית."}],"expected":{"grounded":true,"refusal":false}}
```

Verify each JSONL line is valid JSON: `for l in $(cat fixtures/llm/eval_set.jsonl); do echo "$l" | python -m json.tool >/dev/null; done` should exit 0 across all 5 lines.

Verify each single-shot fixture parses cleanly: `python -m json.tool < fixtures/llm/policy_returns.json` exits 0 (etc).

Note: `fixtures/` lives at REPO ROOT (alongside `scripts/`), NOT under `backend/`. This is intentional — the harness runs from repo root, fixtures are user-facing artifacts. Make sure `.gitignore` does not exclude `fixtures/` (Phase 1 .gitignore allows it).
  </action>
  <verify>
    <automated>cd /Users/stavnsananes/Applications/receptra/.claude/worktrees/practical-franklin-071ba2 && python -m json.tool < fixtures/llm/policy_returns.json > /dev/null && python -m json.tool < fixtures/llm/policy_hours.json > /dev/null && python -m json.tool < fixtures/llm/empty_context.json > /dev/null && python -c "import json; lines = [json.loads(l) for l in open('fixtures/llm/eval_set.jsonl')]; assert len(lines) == 5; assert all('id' in l and 'transcript' in l and 'context' in l and 'expected' in l for l in lines); print('OK 5 lines')"</automated>
  </verify>
  <done>
- 4 fixture files committed to repo root under `fixtures/llm/`.
- All 5 eval-set lines are valid JSON.
- All 3 single-shot fixtures parse cleanly.
- `policy_hours.json` has the same chunk as `policy_returns.json` (the irrelevance is the QUESTION, not the chunk — paired via eval_set entries).
- No fixture is gitignored.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/eval_llm.py — argparse-driven CLI harness with single-shot + eval-set modes</name>
  <files>scripts/eval_llm.py</files>
  <behavior>
    - `python scripts/eval_llm.py --help` exits 0 and prints all 8 documented flags.
    - `python scripts/eval_llm.py --transcript "שלום" --context-file fixtures/llm/empty_context.json --no-audit --no-stream` exits 0; stdout contains `אין לי מספיק מידע`; stderr is empty (no token feed because empty context = short-circuit). NO Ollama call (proven by structurally reaching this path).
    - Single-shot stdout is a single pretty-printed JSON object representing the CompleteEvent (fields: type, suggestions, ttft_ms, total_ms, model). Stderr (when not --no-stream) emits one line per TokenEvent and a final `TTFT: N ms  TOTAL: N ms  MODEL: M  GROUNDED: true|false` summary.
    - Eval-set mode: `python scripts/eval_llm.py --eval-set fixtures/llm/eval_set.jsonl --no-audit --no-stream` runs all 5 lines and prints a final aggregate JSON to stdout (count, mean_ttft_ms, p95_ttft_ms, refusal_rate, grounded_rate, parse_retry_rate, parse_error_rate, pass_rate). When --out-jsonl is provided, each per-line result is also appended to that file.
    - `--no-audit` skips `build_record_call` wiring (no ./data writes).
    - `--ollama-host URL` overrides Settings; `--model TAG` overrides; `--system-prompt-lang en` activates the English system prompt.
    - When `LlmErrorEvent(code='ollama_unreachable')` is received in single-shot mode, exit code is 1.
    - When the parse_error rate in eval-set mode exceeds 5% (i.e., > 1 in 20 = > 5% strict), exit code is 2.
    - Module-level: ZERO imports from `receptra.stt.*` or `faster_whisper` or `silero_vad`. Verified by Task 3 below.
  </behavior>
  <action>
Create `scripts/eval_llm.py` (kept linear / readable; use only stdlib + receptra.llm + receptra.config):

```python
#!/usr/bin/env python3
"""Receptra Phase 3 — LLM Suggestion CLI Harness (LLM-06).

Independent of the STT pipeline by construction: imports only
``receptra.llm.*`` + ``receptra.config``. The structural regression test
``backend/tests/llm/test_harness_isolation.py`` enforces this.

Usage:
    # Single-shot
    python scripts/eval_llm.py \\
        --transcript "תוך כמה זמן אני יכול להחזיר מוצר?" \\
        --context-file fixtures/llm/policy_returns.json

    # Empty context (short-circuit refusal — no Ollama call)
    python scripts/eval_llm.py \\
        --transcript "שלום" \\
        --context-file fixtures/llm/empty_context.json --no-stream

    # Eval set
    python scripts/eval_llm.py \\
        --eval-set fixtures/llm/eval_set.jsonl \\
        --out-jsonl results/llm_eval.jsonl

Exit codes:
    0  success
    1  ollama unreachable / timeout (single-shot only)
    2  parse_error rate > 5% (eval-set only)
"""
from __future__ import annotations

import argparse
import asyncio
import json
import statistics
import sys
from collections.abc import Callable
from pathlib import Path
from typing import Any, TextIO

# IMPORTANT: Do NOT import receptra.stt or faster_whisper or silero_vad here.
# The harness STT-isolation regression test enforces this contract.
from receptra.config import settings
from receptra.llm.engine import generate_suggestions
from receptra.llm.metrics import build_record_call
from receptra.llm.schema import (
    ChunkRef,
    CompleteEvent,
    LlmErrorEvent,
    SuggestionEvent,
    TokenEvent,
)


PARSE_ERROR_RATE_THRESHOLD = 0.05  # exit 2 when > 5% in eval-set mode


# --- Argument parser -------------------------------------------------------


def _make_parser() -> argparse.ArgumentParser:
    p = argparse.ArgumentParser(
        prog="eval_llm.py",
        description="Receptra Phase 3 LLM CLI harness (LLM-06).",
    )
    mode = p.add_mutually_exclusive_group(required=True)
    mode.add_argument("--transcript", help="Single-shot transcript (Hebrew).")
    mode.add_argument(
        "--eval-set", help="Path to a JSONL eval set (one row per line)."
    )
    p.add_argument(
        "--context-file",
        help="Single-shot context JSON file ([{id,text,source?},...]).",
    )
    p.add_argument(
        "--out-jsonl",
        help="Eval-set mode: write per-row results to this JSONL file.",
    )
    p.add_argument("--model", help="Override settings.llm_model_tag.")
    p.add_argument(
        "--ollama-host",
        help="Override settings.ollama_host (e.g. http://localhost:11434).",
    )
    p.add_argument(
        "--system-prompt-lang",
        choices=["he", "en"],
        help="Override settings.llm_system_prompt_lang.",
    )
    p.add_argument(
        "--no-stream",
        action="store_true",
        help="Single-shot: suppress stderr TokenEvent feed.",
    )
    p.add_argument(
        "--no-audit",
        action="store_true",
        help="Skip build_record_call wiring (useful for CI smoke).",
    )
    return p


# --- Fixture loading -------------------------------------------------------


def _load_chunks(path: str | Path) -> list[ChunkRef]:
    raw = json.loads(Path(path).read_text(encoding="utf-8"))
    if not isinstance(raw, list):
        raise ValueError(f"context file {path} must be a JSON array")
    return [
        ChunkRef(
            id=str(item["id"]),
            text=str(item["text"]),
            source=item.get("source"),
        )
        for item in raw
    ]


def _load_eval_set(path: str | Path) -> list[dict[str, Any]]:
    out: list[dict[str, Any]] = []
    with open(path, encoding="utf-8") as fh:
        for raw_line in fh:
            line = raw_line.strip()
            if not line:
                continue
            out.append(json.loads(line))
    return out


# --- Single-shot mode ------------------------------------------------------


async def run_single_shot(
    transcript: str,
    chunks: list[ChunkRef],
    *,
    model: str | None,
    record_call: Callable[..., None] | None,
    stream_to_stderr: bool,
    out: TextIO,
    err: TextIO,
) -> int:
    """Returns exit code (0 success, 1 ollama unreachable/timeout)."""
    if settings.llm_system_prompt_lang not in {"he", "en"}:
        # build_messages will raise; surface a friendly error early.
        print(
            f"ERROR: invalid system_prompt_lang={settings.llm_system_prompt_lang}",
            file=err,
        )
        return 1

    complete: CompleteEvent | None = None
    fatal_code: str | None = None

    async for ev in generate_suggestions(
        transcript, chunks, model=model, record_call=record_call
    ):
        if isinstance(ev, TokenEvent):
            if stream_to_stderr:
                err.write(ev.delta)
                err.flush()
        elif isinstance(ev, CompleteEvent):
            complete = ev
        elif isinstance(ev, LlmErrorEvent):
            print(json.dumps({"type": "error", "code": ev.code, "detail": ev.detail}, ensure_ascii=False), file=err)
            if ev.code in {"ollama_unreachable", "timeout"}:
                fatal_code = ev.code

    if stream_to_stderr:
        err.write("\n")  # newline after token feed

    if complete is None:
        # Reached only via ollama_unreachable / timeout (no terminal CompleteEvent).
        return 1 if fatal_code else 0

    # Pretty-print CompleteEvent JSON to stdout.
    out_payload = {
        "type": "complete",
        "suggestions": [
            {
                "text": s.text,
                "confidence": s.confidence,
                "citation_ids": list(s.citation_ids),
            }
            for s in complete.suggestions
        ],
        "ttft_ms": complete.ttft_ms,
        "total_ms": complete.total_ms,
        "model": complete.model,
    }
    print(json.dumps(out_payload, ensure_ascii=False, indent=2), file=out)

    grounded = any(s.citation_ids for s in complete.suggestions)
    print(
        f"TTFT: {complete.ttft_ms} ms  TOTAL: {complete.total_ms} ms  "
        f"MODEL: {complete.model}  GROUNDED: {str(grounded).lower()}",
        file=err,
    )
    return 1 if fatal_code else 0


# --- Eval-set mode ---------------------------------------------------------


async def _run_one_eval(
    row: dict[str, Any],
    *,
    model: str | None,
    record_call: Callable[..., None] | None,
) -> dict[str, Any]:
    """Run one eval row and return per-row result dict."""
    chunks = [
        ChunkRef(id=str(c["id"]), text=str(c["text"]), source=c.get("source"))
        for c in row.get("context", [])
    ]
    transcript = str(row["transcript"])
    expected = row.get("expected", {})

    complete: CompleteEvent | None = None
    error_code: str | None = None
    saw_parse_retry = False

    async for ev in generate_suggestions(
        transcript, chunks, model=model, record_call=record_call
    ):
        if isinstance(ev, CompleteEvent):
            complete = ev
        elif isinstance(ev, LlmErrorEvent):
            error_code = ev.code
            if ev.code == "parse_error":
                saw_parse_retry = True

    is_refusal = bool(
        complete
        and complete.suggestions
        and complete.suggestions[0].text == "אין לי מספיק מידע"
    )
    is_grounded = bool(
        complete and any(s.citation_ids for s in complete.suggestions)
    )

    expected_refusal = bool(expected.get("refusal", False))
    expected_grounded = bool(expected.get("grounded", False))

    if expected_refusal:
        passed = is_refusal
    elif expected_grounded:
        passed = is_grounded and not is_refusal
    else:
        passed = complete is not None  # at least we got a terminal event

    status = "ok"
    if error_code in {"ollama_unreachable", "timeout"}:
        status = error_code
    elif saw_parse_retry and complete and is_refusal and not expected_refusal:
        status = "parse_error"
    elif saw_parse_retry and complete and not is_refusal:
        # Retry recovered to a valid reply
        status = "parse_retry_ok"

    return {
        "id": row.get("id"),
        "ttft_ms": complete.ttft_ms if complete else -1,
        "total_ms": complete.total_ms if complete else -1,
        "status": status,
        "is_refusal": is_refusal,
        "is_grounded": is_grounded,
        "passed": passed,
        "suggestions": [
            {"text": s.text, "confidence": s.confidence, "citation_ids": list(s.citation_ids)}
            for s in (complete.suggestions if complete else [])
        ],
        "error_code": error_code,
    }


def _aggregate(rows: list[dict[str, Any]]) -> dict[str, Any]:
    n = len(rows)
    if n == 0:
        return {"count": 0}
    ttfts = [r["ttft_ms"] for r in rows if r["ttft_ms"] >= 0]
    refusals = sum(1 for r in rows if r["is_refusal"])
    grounded = sum(1 for r in rows if r["is_grounded"])
    parse_retries = sum(1 for r in rows if r["status"] == "parse_retry_ok")
    parse_errors = sum(1 for r in rows if r["status"] == "parse_error")
    passes = sum(1 for r in rows if r["passed"])

    def _p95(xs: list[int]) -> float:
        if not xs:
            return -1.0
        xs_sorted = sorted(xs)
        idx = max(0, int(round(0.95 * (len(xs_sorted) - 1))))
        return float(xs_sorted[idx])

    return {
        "count": n,
        "mean_ttft_ms": statistics.mean(ttfts) if ttfts else -1,
        "p95_ttft_ms": _p95(ttfts),
        "refusal_rate": refusals / n,
        "grounded_rate": grounded / n,
        "parse_retry_rate": parse_retries / n,
        "parse_error_rate": parse_errors / n,
        "pass_rate": passes / n,
    }


async def run_eval_set(
    eval_path: str,
    out_jsonl: str | None,
    *,
    model: str | None,
    record_call: Callable[..., None] | None,
    out: TextIO,
    err: TextIO,
) -> int:
    """Returns exit code (0 success, 1 ollama unreachable, 2 parse_error rate > threshold)."""
    rows = _load_eval_set(eval_path)
    results: list[dict[str, Any]] = []
    out_handle = open(out_jsonl, "w", encoding="utf-8") if out_jsonl else None
    try:
        for row in rows:
            result = await _run_one_eval(row, model=model, record_call=record_call)
            results.append(result)
            if out_handle is not None:
                out_handle.write(json.dumps(result, ensure_ascii=False) + "\n")
                out_handle.flush()
            print(json.dumps({"id": result["id"], "passed": result["passed"], "status": result["status"]}, ensure_ascii=False), file=err)
    finally:
        if out_handle is not None:
            out_handle.close()

    agg = _aggregate(results)
    print(json.dumps(agg, ensure_ascii=False, indent=2), file=out)

    if any(r["status"] in {"ollama_unreachable", "timeout"} for r in results):
        return 1
    if agg.get("parse_error_rate", 0.0) > PARSE_ERROR_RATE_THRESHOLD:
        return 2
    return 0


# --- Main ------------------------------------------------------------------


async def _amain(args: argparse.Namespace) -> int:
    # Apply CLI overrides on settings (Pydantic BaseSettings: assign with care —
    # we use ``object.__setattr__`` because BaseSettings instances are not
    # frozen but assignments are validated; we round-trip to validate).
    if args.ollama_host:
        # Direct attribute assignment via __dict__ to skip pydantic validation
        # (we trust the URL; settings is loaded once at process start).
        settings.__dict__["ollama_host"] = args.ollama_host
    if args.system_prompt_lang:
        settings.__dict__["llm_system_prompt_lang"] = args.system_prompt_lang

    record_call = None
    if not args.no_audit:
        record_call = build_record_call(settings.audit_db_path)

    if args.transcript is not None:
        if not args.context_file:
            print("ERROR: --context-file is required with --transcript", file=sys.stderr)
            return 1
        chunks = _load_chunks(args.context_file)
        return await run_single_shot(
            args.transcript,
            chunks,
            model=args.model,
            record_call=record_call,
            stream_to_stderr=not args.no_stream,
            out=sys.stdout,
            err=sys.stderr,
        )
    else:  # --eval-set
        return await run_eval_set(
            args.eval_set,
            args.out_jsonl,
            model=args.model,
            record_call=record_call,
            out=sys.stdout,
            err=sys.stderr,
        )


def main() -> int:
    args = _make_parser().parse_args()
    return asyncio.run(_amain(args))


if __name__ == "__main__":
    sys.exit(main())
```

Make the file executable: `chmod +x scripts/eval_llm.py`.
  </action>
  <verify>
    <automated>cd /Users/stavnsananes/Applications/receptra/.claude/worktrees/practical-franklin-071ba2/backend && uv run --project . python ../scripts/eval_llm.py --help && uv run --project . python ../scripts/eval_llm.py --transcript "שלום" --context-file ../fixtures/llm/empty_context.json --no-audit --no-stream | grep -q "אין לי מספיק מידע"</automated>
  </verify>
  <done>
- `python scripts/eval_llm.py --help` prints argparse help with all 8 flags.
- `--transcript "שלום" --context-file empty_context.json --no-audit --no-stream` exits 0 and stdout contains `אין לי מספיק מידע` (proves end-to-end short-circuit path runs).
- ruff + mypy strict clean (mypy may not cover scripts/ — confirm with `uv run mypy scripts/eval_llm.py`).
- File is executable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Harness STT-isolation regression test (LLM-06 structural lock)</name>
  <files>backend/tests/llm/test_harness_isolation.py</files>
  <action>
Create `backend/tests/llm/test_harness_isolation.py`:

```python
"""LLM-06 structural regression: scripts/eval_llm.py harness MUST NOT pull
in receptra.stt or any STT-only deps (faster_whisper, silero_vad, torch,
onnxruntime, ctranslate2, av).

Runs the harness module in a SUBPROCESS so any module-state pollution from
other tests in this session cannot mask a real regression.

If this fails, the most likely culprits are:
- Someone added `from receptra.stt.* import ...` to receptra.llm.*
- Someone added `import faster_whisper` to a shared module
- A circular import in receptra/__init__.py started executing STT init
"""
from __future__ import annotations

import os
import subprocess
import sys
from pathlib import Path

import pytest


_HARNESS_PATH = Path(__file__).resolve().parents[3] / "scripts" / "eval_llm.py"
_REPO_ROOT = Path(__file__).resolve().parents[3]


# Modules whose presence in sys.modules indicates an isolation regression.
_FORBIDDEN_PREFIXES = (
    "receptra.stt",
    "faster_whisper",
    "silero_vad",
    "torch",  # heavy; pulled by silero
    "onnxruntime",  # pulled by silero
    "ctranslate2",  # pulled by faster_whisper
    "av",  # pulled by faster_whisper
)


def test_harness_module_is_stt_clean() -> None:
    """Importing the harness module must not load any STT-domain modules."""
    if not _HARNESS_PATH.exists():
        pytest.skip(f"harness not at {_HARNESS_PATH}")

    code = (
        "import sys, json\n"
        "import importlib.util\n"
        f"spec = importlib.util.spec_from_file_location('eval_llm', {str(_HARNESS_PATH)!r})\n"
        "mod = importlib.util.module_from_spec(spec)\n"
        "assert spec.loader is not None\n"
        "spec.loader.exec_module(mod)\n"
        f"forbidden = {_FORBIDDEN_PREFIXES!r}\n"
        "leaked = sorted(k for k in sys.modules if any(k == p or k.startswith(p + '.') for p in forbidden))\n"
        "print(json.dumps({'leaked': leaked}))\n"
    )

    proc = subprocess.run(
        [sys.executable, "-c", code],
        capture_output=True, text=True, timeout=30, check=False,
        cwd=str(_REPO_ROOT),
        env={
            **os.environ,
            # Belt + braces: ensure the subprocess does not load .env auto-magic
            # that might side-trigger imports.
            "RECEPTRA_LLM_LIVE_TEST": "",
        },
    )

    assert proc.returncode == 0, (
        f"harness import subprocess failed:\nstdout={proc.stdout}\nstderr={proc.stderr}"
    )

    last_line = proc.stdout.strip().splitlines()[-1]
    import json as _json

    payload = _json.loads(last_line)
    assert payload["leaked"] == [], (
        f"LLM-06 isolation contract violated: scripts/eval_llm.py transitively "
        f"imported {payload['leaked']}. Find the import in receptra.llm.* or "
        f"the harness itself and remove it. (Don't ignore — LLM-06 requires "
        f"the CLI harness to be independent of the STT pipeline.)"
    )


def test_engine_module_is_stt_clean() -> None:
    """Belt + braces: also verify importing receptra.llm.engine alone is STT-clean.

    The harness test above is the user-facing contract; this is the lower-level
    canary so a regression in receptra.llm.engine surfaces here even if the
    harness imports something else.
    """
    code = (
        "import sys, json\n"
        "import receptra.llm.engine  # noqa\n"
        f"forbidden = {_FORBIDDEN_PREFIXES!r}\n"
        "leaked = sorted(k for k in sys.modules if any(k == p or k.startswith(p + '.') for p in forbidden))\n"
        "print(json.dumps({'leaked': leaked}))\n"
    )
    proc = subprocess.run(
        [sys.executable, "-c", code],
        capture_output=True, text=True, timeout=30, check=False,
        cwd=str(_REPO_ROOT / "backend"),
        env={**os.environ, "PYTHONPATH": str(_REPO_ROOT / "backend" / "src")},
    )
    assert proc.returncode == 0, f"engine subprocess failed:\n{proc.stderr}"
    last_line = proc.stdout.strip().splitlines()[-1]
    import json as _json

    payload = _json.loads(last_line)
    assert payload["leaked"] == [], (
        f"LLM-06 contract violated at engine level: receptra.llm.engine pulled "
        f"{payload['leaked']}. Plan 03-04 boundary regressed."
    )
```
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/llm/test_harness_isolation.py -x -v --tb=short</automated>
  </verify>
  <done>
- Both isolation tests pass (no `receptra.stt`, `faster_whisper`, `silero_vad`, `torch`, `onnxruntime`, `ctranslate2`, `av` in sys.modules after harness/engine import).
- Tests run in subprocesses so they cannot be polluted by other tests' module imports.
- ruff + mypy strict clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: docs/llm.md — Phase 3 user/contributor doc (≥150 lines)</name>
  <files>docs/llm.md</files>
  <action>
Create `docs/llm.md` mirroring the structure of `docs/stt.md` (Plan 02-06). Required sections:

1. **# Receptra Phase 3 — Hebrew Suggestion LLM**
2. **## Overview** — Phase 3 ships a local Hebrew suggestion engine (DictaLM 3.0 via Ollama, Qwen 2.5 7B fallback) + structured JSON output + grounded refusal contract. NO public HTTP/WebSocket route in this phase — the CLI harness is the only external entry. Phase 5 mounts the engine onto `/ws/agent`.
3. **## Internal Interface Contract** — `generate_suggestions(transcript, context_chunks, *, request_id=None, model=None, record_call=None) -> AsyncGenerator[SuggestionEvent, None]`. Document each event type (TokenEvent / CompleteEvent / LlmErrorEvent codes Literal allowlist) with one-line semantics. Include the `_CANONICAL_REFUSAL` constant.
4. **## CLI Usage** — both modes with examples:
   ```bash
   # Single-shot
   uv run --project backend python scripts/eval_llm.py \
       --transcript "תוך כמה זמן אני יכול להחזיר מוצר?" \
       --context-file fixtures/llm/policy_returns.json

   # Eval set
   uv run --project backend python scripts/eval_llm.py \
       --eval-set fixtures/llm/eval_set.jsonl \
       --out-jsonl results/llm_eval.jsonl
   ```
   Document fixture format, exit codes, the 5% parse-error threshold (exit 2).
5. **## Audit Log + PII Warning** — explain `event="llm.call"` JSON shape; `transcript_hash` is sha256[:16] (8 bytes prefix); raw transcript NEVER stored in SQLite; `RECEPTRA_LLM_LOG_TEXT_REDACTION_DISABLED=true` opt-in includes transcript in loguru output (DO NOT enable in shared environments). Include WARNING block:
   ```
   WARNING: data/audit.sqlite carries `transcript_hash` (sha256[:16]) and full
   timing + status data. Do NOT attach this file to public bug reports without
   first verifying you have not enabled RECEPTRA_LLM_LOG_TEXT_REDACTION_DISABLED.
   ```
6. **## Grounding Contract (LLM-03)** — three layered defenses:
   1. Hard short-circuit on empty context (zero Ollama calls)
   2. Explicit refusal instruction in system prompt
   3. Few-shot example #2 demonstrates refusal JSON shape

   Canonical refusal text: `"אין לי מספיק מידע"`. Live test `test_grounding_refusal_on_irrelevant_context_live` enforces this on the developer's machine.
7. **## Live Tests** — `cd backend && RECEPTRA_LLM_LIVE_TEST=1 uv run pytest tests/llm/ -x -v -m live`. Lists requirements: host `ollama serve` running, `dictalm3` registered (or `qwen2.5:7b` fallback). ChatML auto-detection grep gate (`test_dictalm3_chatml_template_detected`) is the recovery anchor for Pitfall B / Assumption A5.
8. **## Known Limitations**:
   - DictaLM 3.0 is non-deterministic at temperature=0 across versions (DictaLM is a real model; small variations expected)
   - `format=schema` deliberately NOT used while streaming (RESEARCH §3.5 / Pitfall A — Ollama upstream issues #14440/#15260)
   - System prompt is Hebrew by default; English variant available via `RECEPTRA_LLM_SYSTEM_PROMPT_LANG=en` (Phase 7 A/B; not production-tuned)
   - Prompt injection via context text: accepted-risk for v1 (industry consensus 2026 — no general defense; Phase 7 polish revisits)
   - Self-reported confidence (not logprob-derived); Phase 7 instruments logprobs in audit row for calibration analysis
   - First Ollama call after process restart: ~5 s cold start (`keep_alive=-1` in DictaLM3.Modelfile pins weights afterwards)
9. **## Troubleshooting**:
   - **`code='ollama_unreachable'`** → check `ollama serve` is running on host; verify `RECEPTRA_OLLAMA_HOST` matches; from Docker, `extra_hosts: host.docker.internal:host-gateway` must be present in compose (Plan 01-04)
   - **`code='ollama_unreachable'` with `detail='model_missing:'`** → run `make models dictalm` (or `make models qwen-fallback`); verify with `ollama list`
   - **High `parse_error_rate` in eval-set output** → likely a prompt regression or DictaLM version drift; run `ollama show dictalm3 --modelfile | grep im_start` to verify ChatML template still auto-detected (Pitfall B)
   - **TTFT > 1 s on warm calls** → check `ollama ps` shows DictaLM still resident; if evicted, increase memory or use Qwen 2.5 7B fallback (5 GB vs 7.5 GB DictaLM Q4)
   - **ChatML grep test fails** → add explicit `TEMPLATE` block to `scripts/ollama/DictaLM3.Modelfile` (recipe in `test_chat_template_grep.py` docstring); re-run `make models dictalm`
   - **Audit DB not writing** → `data/` directory permissions; verify `./data:/app/data:rw` volume in docker-compose (Plan 02-06); check `RECEPTRA_AUDIT_DB_PATH` env var
10. **## Cross-references** — link to:
    - `.planning/phases/03-hebrew-suggestion-llm/03-RESEARCH.md` (canonical research)
    - `.planning/phases/03-hebrew-suggestion-llm/03-01-SUMMARY.md` through `03-06-SUMMARY.md`
    - `docs/stt.md` (parallel Phase 2 doc)

Target ≥ 150 lines. Use code fences for shell examples, tables for the LlmErrorEvent code allowlist + Settings env vars, and explicit ⚠ for PII warnings.
  </action>
  <verify>
    <automated>cd /Users/stavnsananes/Applications/receptra/.claude/worktrees/practical-franklin-071ba2 && test -f docs/llm.md && [ "$(wc -l < docs/llm.md)" -ge 150 ] && grep -q "אין לי מספיק מידע" docs/llm.md && grep -qi "transcript_hash" docs/llm.md && grep -q "RECEPTRA_LLM_LOG_TEXT_REDACTION_DISABLED" docs/llm.md && grep -q "ollama_unreachable" docs/llm.md && grep -q "ChatML" docs/llm.md</automated>
  </verify>
  <done>
- `docs/llm.md` exists at ≥150 lines.
- All 10 sections present.
- Canonical refusal `"אין לי מספיק מידע"` appears in the doc (grounding contract documented).
- PII warning block surfaced with `RECEPTRA_LLM_LOG_TEXT_REDACTION_DISABLED` env-var name.
- Troubleshooting covers all 6 named scenarios.
- Cross-references to RESEARCH and Plan SUMMARYs are present.
- ChatML troubleshooting recipe present (Pitfall B recovery).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI invocation → harness module load | Module imports must stay STT-clean (LLM-06 contract) |
| developer-supplied context-file JSON → ChunkRef construction | Untrusted JSON enters; `_load_chunks` validates shape |
| eval-set JSONL → engine input | Each row validated implicitly by ChunkRef + DoS bounds in Plan 03-02 |
| harness → settings (overridden by --ollama-host / --model / --system-prompt-lang) | CLI flags can override Settings; via __dict__ to skip pydantic; we trust developer-supplied URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | E (Elevation of privilege) | Harness silently importing `receptra.stt` and pulling Whisper into the LLM-only environment | mitigate | LOCKED: subprocess isolation test in `test_harness_isolation.py` enumerates 6 forbidden module prefixes (`receptra.stt`, `faster_whisper`, `silero_vad`, `torch`, `onnxruntime`, `ctranslate2`, `av`); test fails CI on first regression. Belt-and-braces second test on `receptra.llm.engine` import. |
| T-03-06-02 | T (Tampering) | Malformed context-file JSON crashing the harness with a confusing stack | mitigate | `_load_chunks` raises ValueError with the file path on wrong shape; argparse + pydantic ChunkRef construction surface field errors at the boundary. The harness exits with a clear message rather than a Python stacktrace. |
| T-03-06-03 | I (Information disclosure) | Harness writing transcript text to stdout/stderr in non-redacted form | mitigate | Single-shot stdout prints CompleteEvent (suggestions + timings — no transcript echo); stderr token feed prints model OUTPUT (suggestion deltas), not the input transcript. Eval-set output prints suggestions + timings + status. The transcript NEVER round-trips to stdout/stderr. Audit goes through `build_record_call` → metrics.py PII redaction. |
| T-03-06-04 | D (Denial of service) | Eval-set with malicious row exceeding DoS bounds halting the run | mitigate | Each row goes through `generate_suggestions`; Plan 03-04 catches `ValueError` from `build_user_message` and yields `LlmErrorEvent(code='no_context')`. The harness records this row as failed and continues to the next row. One bad row never blocks the eval. |
| T-03-06-05 | T (Tampering) | `--ollama-host` flag pointing at an arbitrary URL | accept | Trust boundary: developer running the harness can already override `RECEPTRA_OLLAMA_HOST`. CLI flag is operationally equivalent. Documented in `docs/llm.md`. |
| T-03-06-06 | I (Information disclosure) | Eval-set `--out-jsonl` file containing suggestion text + transcript hash on a shared filesystem | accept | Output file location is developer-supplied; same trust as `data/audit.sqlite`. The output JSONL contains suggestion text (not transcript) + transcript_hash + status. Documented in `docs/llm.md` audit section. |
| T-03-06-07 | T (Tampering) | Phase 4/5/6 silently regressing the LLM-06 isolation contract by adding STT imports to receptra.llm.* | mitigate | Subprocess regression test runs in CI on every push; first regression fails the build. Test message names the leaked module(s) for fast triage. |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/llm/ -x -v` green (~75+ tests across all 6 plans cumulatively).
- `cd backend && uv run pytest tests/ -x` green (full backend suite — STT + LLM + healthz).
- `cd backend && uv run python ../scripts/eval_llm.py --transcript "שלום" --context-file ../fixtures/llm/empty_context.json --no-audit --no-stream` exits 0 and stdout contains `"text": "אין לי מספיק מידע"`.
- `python -c "import sys, importlib.util; spec=importlib.util.spec_from_file_location('h','scripts/eval_llm.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert not any(k.startswith('receptra.stt') for k in sys.modules); print('STT-clean')"` from repo root prints `STT-clean`.
- `bash scripts/check_licenses.sh` exits 0 (Phase 3 added only `ollama` MIT — already allowlisted; no new licenses introduced by this plan).
- `cd /repo && uv run --project backend python scripts/eval_llm.py --eval-set fixtures/llm/eval_set.jsonl --no-audit --no-stream` (with live Ollama) exits 0 and prints aggregate stats with `pass_rate >= 0.6` (3/5 of the eval rows must pass even on a slightly-degraded model — empty-context refusal and short-circuit always pass; the irrelevant-context refusal is the live-grounding bar).
- ruff + mypy strict clean across `backend/src/receptra/llm`, `backend/tests/llm`, `scripts/eval_llm.py`.
- `docs/llm.md` ≥ 150 lines and contains the 6 troubleshooting scenarios.
</verification>

<success_criteria>
- LLM-06 delivered: CLI harness ships at `scripts/eval_llm.py`, structurally STT-clean (regression-tested in subprocess).
- 4 fixtures committed (`fixtures/llm/`) — Phase 7 prompt-tuning extends `eval_set.jsonl` from 5 to 20 lines.
- `docs/llm.md` parallels `docs/stt.md` and serves as the Phase 3 contract surface for users + contributors.
- Phase 3 closes: 6/6 LLM-* requirements delivered (LLM-01 plans 03-01 + 03-03; LLM-02 plan 03-04; LLM-03 plans 03-02 + 03-04; LLM-04 plans 03-02 + 03-04; LLM-05 plan 03-05; LLM-06 plan 03-06).
- The harness composes `generate_suggestions` + `build_record_call` + Settings overrides — same composition Phase 5 hot-path will mirror, just on a different transport (WebSocket instead of stdout).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hebrew-suggestion-llm/03-06-SUMMARY.md` covering:
- Final line count of `docs/llm.md`.
- Result of `python scripts/eval_llm.py --transcript ... --context-file fixtures/llm/empty_context.json` smoke (exit code + stdout contains canonical refusal).
- Subprocess isolation test result on the executor's machine.
- Live eval-set run result (if RECEPTRA_LLM_LIVE_TEST=1 was set on the executor): pass_rate, mean_ttft_ms, p95_ttft_ms.
- Confirmation that Phase 3 closes — 6/6 LLM-* requirements delivered.
- Any deferred work (e.g., Phase 7 will grow eval_set.jsonl to 20 lines).
</output>
