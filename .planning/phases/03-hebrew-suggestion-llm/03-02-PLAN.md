---
phase: 03-hebrew-suggestion-llm
plan: 03-02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/src/receptra/llm/__init__.py
  - backend/src/receptra/llm/schema.py
  - backend/src/receptra/llm/prompts.py
  - backend/tests/llm/test_schema.py
  - backend/tests/llm/test_prompts.py
autonomous: true
requirements: [LLM-03, LLM-04]
tags: [llm, schema, pydantic, prompts, hebrew, chatml, few-shot, grounding]

must_haves:
  truths:
    - "`receptra.llm.schema` publishes `Suggestion`, `SuggestionResponse`, `TokenEvent`, `CompleteEvent`, `LlmErrorEvent`, and `SuggestionEvent` (TypeAdapter-friendly discriminated union on field `type`)"
    - "All schema models are `frozen=True, extra='forbid'` (mirrors Plan 02-04 SttEvent contract)"
    - "Suggestion text bound 0 < len ≤ 280 chars; confidence ∈ [0.0, 1.0]; citation_ids is list[str] (default empty)"
    - "SuggestionResponse.suggestions is min_length=1, max_length=3"
    - "ChunkRef is published as a frozen dataclass in `receptra.llm.schema` with fields `id: str` + `text: str` + optional `source: dict | None = None` (Phase 3 owns the contract; Phase 4 RAG re-exports as `ChunkRef`)"
    - "`receptra.llm.prompts` publishes `SYSTEM_PROMPT_HE` (Hebrew system prompt VERBATIM from RESEARCH §5.2), `SYSTEM_PROMPT_EN` (placeholder English variant), `FEW_SHOTS_HE` (2 alternating user/assistant turns from RESEARCH §5.4), `build_user_message`, and `build_messages`"
    - "`build_messages(transcript, context_chunks, lang='he')` returns a list shaped: [system, fewshot_user_1, fewshot_asst_1, fewshot_user_2, fewshot_asst_2, final_user] — exactly 6 messages"
    - "`build_user_message` renders empty context as `<context>\\n(אין קטעי הקשר זמינים)\\n</context>` and non-empty as `[id: <id>]\\n<text>` blocks separated by blank lines, all wrapped in <context>...</context> + <transcript>...</transcript>"
    - "`build_user_message` enforces transcript length bound (`len(transcript) ≤ 2000` chars; raises ValueError otherwise — Threat T-03-LLM-DoS)"
    - "`build_user_message` enforces context bounds (`len(context_chunks) ≤ 10` AND `sum(len(c.text)) ≤ 12000` chars; raises ValueError otherwise — Threat T-03-LLM-DoS)"
  artifacts:
    - path: "backend/src/receptra/llm/__init__.py"
      provides: "Package marker; re-exports schema + prompts public surface"
      exports: ["Suggestion", "SuggestionResponse", "SuggestionEvent", "ChunkRef"]
    - path: "backend/src/receptra/llm/schema.py"
      provides: "Pydantic v2 schema for suggestions + streaming events + ChunkRef dataclass"
      exports: ["Suggestion", "SuggestionResponse", "TokenEvent", "CompleteEvent", "LlmErrorEvent", "SuggestionEvent", "ChunkRef"]
    - path: "backend/src/receptra/llm/prompts.py"
      provides: "Hebrew system prompt + 2 few-shot turns + build_user_message + build_messages"
      exports: ["SYSTEM_PROMPT_HE", "SYSTEM_PROMPT_EN", "FEW_SHOTS_HE", "build_user_message", "build_messages"]
    - path: "backend/tests/llm/test_schema.py"
      provides: "Schema invariants + discriminated-union round-trip tests"
    - path: "backend/tests/llm/test_prompts.py"
      provides: "Prompt-builder structure + Hebrew text byte-exact preservation + DoS-bound tests"
  key_links:
    - from: "backend/src/receptra/llm/schema.py"
      to: "pydantic v2 + TypeAdapter discriminated union"
      via: "Annotated[Union[...], Field(discriminator='type')]"
      pattern: "discriminator"
    - from: "backend/src/receptra/llm/prompts.py"
      to: "receptra.llm.schema.ChunkRef"
      via: "build_user_message(transcript, context_chunks: list[ChunkRef])"
      pattern: "ChunkRef"
---

<objective>
Lay down the typed contracts and Hebrew prompt scaffolding for the suggestion engine. This is a pure-data + pure-string plan — no Ollama calls, no I/O, no async. The output is the immutable Pydantic schema that Plans 03-03 (client), 03-04 (engine), 03-05 (metrics + audit), 03-06 (CLI harness), Phase 4 (RAG calls `generate_suggestions`), Phase 5 (hot path muxer), and Phase 6 (frontend codegen) all consume.

Purpose: LLM-03 (grounded refusal — the system prompt + few-shot example #2 hardcode the canonical "אין לי מספיק מידע" response shape) + LLM-04 (structured JSON `suggestions[]` with text/confidence/citation_ids — pydantic v2 model with frozen + extra=forbid is the parser contract). Mirrors Plan 02-04's SttEvent discriminated-union pattern exactly so Phase 5 readers don't context-switch.

Output: the `receptra.llm` package skeleton (created here for the first time), `schema.py` + `prompts.py`, and ~15 tests covering schema invariants + Hebrew prompt byte-exactness + DoS bounds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-hebrew-suggestion-llm/03-RESEARCH.md
@.planning/phases/02-hebrew-streaming-stt/02-04-PLAN.md
@backend/src/receptra/stt/events.py

<interfaces>
Consumed from Plan 03-01 (Wave 1):
- `settings.llm_system_prompt_lang` — read by callers of `build_messages`; this plan accepts the lang param explicitly so prompts.py stays stateless (no `import settings`).
- `backend/tests/llm/conftest.py::live_test_enabled` — not used by this plan (no live tests).

Consumed from Phase 2 (already shipped) for STYLE reference (NOT import):
- `backend/src/receptra/stt/events.py` — `SttEvent = Annotated[Union[...], Field(discriminator='type')]` is the EXACT pattern to mirror for `SuggestionEvent`.
- pydantic v2 + ConfigDict(frozen=True, extra='forbid') is the codebase convention.

Published for Plan 03-03 (Wave 2 parallel) — they only need the types:
- `Suggestion`, `SuggestionResponse` for parse target
- `TokenEvent`, `CompleteEvent`, `LlmErrorEvent`, `SuggestionEvent` discriminated-union for the engine's AsyncGenerator
- `ChunkRef` dataclass — engine accepts `list[ChunkRef]` as the context-chunks input

Published for Plan 03-04 (Wave 3) — engine orchestration consumes:
- `build_messages(transcript, context_chunks, lang)` returns the exact list[dict[str,str]] passed to `client.chat(messages=...)`
- `Suggestion`/`SuggestionResponse` for `model_validate_json(stripped_completion)`
- The three Event types yielded as `AsyncGenerator[SuggestionEvent, None]`

Published for Phase 5 hot path (future):
- `SuggestionEvent` TypeAdapter for WebSocket muxing onto `/ws/agent`
- `LlmErrorEvent.code` Literal allowlist (4 values) — adding a 5th requires plan amendment so consumer switches stay total

LOCKED CONTENT (use VERBATIM — no rewording):

**SYSTEM_PROMPT_HE** (RESEARCH §5.2):
```
אתה עוזר וירטואלי לסוכן שירות לקוחות בשיחה טלפונית בעברית.
תפקידך: לקרוא את התמלול של מה שהלקוח אמר ולהציע לסוכן עד שלוש תשובות קצרות, מדויקות, ובסגנון אנושי בעברית.

חוקים מחייבים:
1. השתמש אך ורק במידע שמופיע בקטעי הידע המסומנים <context>...</context>. אל תמציא עובדות עסקיות.
2. אם המידע ב-<context> אינו מספיק כדי לענות, החזר תשובה אחת בלבד עם הטקסט "אין לי מספיק מידע" וציטוטים ריקים.
3. כל תשובה חייבת להיות עד 280 תווים, בעברית טבעית, מנוסחת כמו אדם אמיתי.
4. החזר את התשובה כ-JSON תקין בלבד, ללא טקסט נוסף לפני או אחרי, בפורמט:
{"suggestions":[{"text":"...","confidence":0.0,"citation_ids":["..."]}]}
5. citation_ids חייב להכיל את ה-id המדויק של קטע ה-<context> שעליו התשובה מבוססת.
6. confidence הוא מספר בין 0.0 ל-1.0 שמשקף עד כמה הקטעים מספקים מענה ישיר.
```

**FEW_SHOTS_HE** (RESEARCH §5.4) — exactly 4 entries (user1, asst1, user2, asst2). User contents wrap context + transcript. Assistant contents are ONLY the JSON line.

Example 1 user content (literal):
```
<context>
[id: kb-policy-returns]
מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.
</context>

<transcript>
תוך כמה זמן אני יכול להחזיר מוצר?
</transcript>
```

Example 1 assistant content (literal, single line):
```
{"suggestions":[{"text":"ניתן להחזיר את המוצר תוך 14 ימים מיום הקנייה, ויש להציג את החשבונית המקורית.","confidence":0.95,"citation_ids":["kb-policy-returns"]}]}
```

Example 2 user content (literal):
```
<context>
[id: kb-policy-returns]
מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.
</context>

<transcript>
מה שעות הפעילות של החנות?
</transcript>
```

Example 2 assistant content (literal):
```
{"suggestions":[{"text":"אין לי מספיק מידע","confidence":0.0,"citation_ids":[]}]}
```

**SYSTEM_PROMPT_EN** (placeholder English variant — Phase 7 A/B owns real tuning; Plan 03-02 ships a faithful translation so the `lang='en'` switch does not crash):
```
You are a virtual assistant for a customer-service agent on a Hebrew phone call.
Your task: read the transcript of what the customer said and propose up to three short, accurate, human-sounding Hebrew replies for the agent.

Mandatory rules:
1. Use ONLY information appearing inside <context>...</context> blocks. Do not fabricate business facts.
2. If <context> is insufficient, return exactly one suggestion with text "אין לי מספיק מידע" and empty citation_ids.
3. Each suggestion must be ≤ 280 characters, in natural Hebrew, phrased like a real person.
4. Return ONLY valid JSON, with no text before or after, in the format:
{"suggestions":[{"text":"...","confidence":0.0,"citation_ids":["..."]}]}
5. citation_ids MUST contain the exact id of the <context> block your suggestion is grounded in.
6. confidence is a 0.0-1.0 float reflecting how directly the chunks answer the question.
```

DoS bounds (Threat T-03-LLM-DoS, RESEARCH §Security Domain):
- `len(transcript) > 2000` chars → raise ValueError("transcript exceeds 2000 chars (DoS guard, see 03-RESEARCH §Security Domain)")
- `len(context_chunks) > 10` → raise ValueError
- `sum(len(c.text) for c in context_chunks) > 12000` → raise ValueError
- These bounds are pre-LLM (in `build_user_message`) so the engine never sends a giant prompt to Ollama.

ChunkRef shape (Phase 3 owns; Phase 4 re-exports):
```python
@dataclass(frozen=True)
class ChunkRef:
    id: str
    text: str
    source: dict[str, str] | None = None  # filename + offset metadata; opaque to LLM
```

`source` is opaque to the LLM (not rendered into the prompt — LLM sees `[id: ...]` + text). Frontend uses it for citation chips in Phase 6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: receptra.llm.schema — Suggestion + SuggestionResponse + Event union + ChunkRef</name>
  <files>backend/src/receptra/llm/__init__.py, backend/src/receptra/llm/schema.py, backend/tests/llm/test_schema.py</files>
  <behavior>
    - `Suggestion(text, confidence, citation_ids=[])` accepts valid Hebrew text; rejects empty string (`min_length=1`); rejects >280 chars; rejects confidence <0 or >1; rejects extra fields with `extra='forbid'`.
    - `SuggestionResponse(suggestions=[Suggestion(...)])` accepts 1-3 entries; rejects 0 or 4+; rejects extra fields.
    - `Suggestion` and `SuggestionResponse` are `frozen=True` — assigning to `.text` after construction raises ValidationError (frozen).
    - `model_validate_json('{"suggestions":[{"text":"שלום","confidence":0.9,"citation_ids":[]}]}')` returns a SuggestionResponse with byte-exact Hebrew preservation through the round-trip.
    - `model_validate_json('{"suggestions":[{"text":"x","confidence":0.5,"citation_ids":[],"extra":"x"}]}')` raises ValidationError (extra='forbid').
    - `TokenEvent(delta="...")`, `CompleteEvent(suggestions=[...], ttft_ms=312, total_ms=1845, model="dictalm3")`, `LlmErrorEvent(code="parse_error", detail="...")` all construct cleanly; `code` is a Literal — assigning `code="random"` rejects.
    - `LlmErrorEvent.code` Literal allowlist is exactly `("ollama_unreachable", "parse_error", "timeout", "no_context")` — adding a 5th value requires plan amendment.
    - `SuggestionEvent` discriminated-union round-trips: `TypeAdapter(SuggestionEvent).validate_python({"type":"token","delta":"x"})` returns a TokenEvent; `validate_python({"type":"complete", ...})` returns a CompleteEvent; `validate_python({"type":"unknown"})` raises ValidationError.
    - `ChunkRef(id="kb-1", text="...")` is a frozen dataclass; `source` defaults to None.
    - `__init__.py` re-exports the public surface so callers can `from receptra.llm import Suggestion`.
  </behavior>
  <action>
Create `backend/src/receptra/llm/__init__.py`:

```python
"""Phase 3 — Hebrew Suggestion LLM package.

Public surface:
    Suggestion, SuggestionResponse — pydantic v2 models for parse target (LLM-04)
    TokenEvent, CompleteEvent, LlmErrorEvent, SuggestionEvent — streaming event union (LLM-02)
    ChunkRef — context-chunk dataclass (Phase 4 RAG re-exports)

Plans 03-03 (client), 03-04 (engine), 03-05 (metrics + audit), 03-06 (CLI harness)
add modules to this package.
"""
from __future__ import annotations

from receptra.llm.schema import (
    ChunkRef,
    CompleteEvent,
    LlmErrorEvent,
    Suggestion,
    SuggestionEvent,
    SuggestionResponse,
    TokenEvent,
)

__all__ = [
    "ChunkRef",
    "CompleteEvent",
    "LlmErrorEvent",
    "Suggestion",
    "SuggestionEvent",
    "SuggestionResponse",
    "TokenEvent",
]
```

Create `backend/src/receptra/llm/schema.py` (mirrors `backend/src/receptra/stt/events.py` styling):

```python
"""Pydantic v2 schema for the Hebrew suggestion engine (LLM-03 + LLM-04).

The schema is the public contract for:
- Plan 03-04 engine — yields ``SuggestionEvent`` instances
- Plan 03-06 CLI harness — pretty-prints CompleteEvent
- Phase 4 RAG — accepts ``list[ChunkRef]`` as input to ``generate_suggestions``
- Phase 5 hot path — TypeAdapter-validates events on the WebSocket muxer
- Phase 6 frontend — codegen-derived TypeScript types (Phase 6's concern)

Every model is ``frozen=True, extra='forbid'`` to mirror Plan 02-04's
``SttEvent`` contract: total switches in consumer code, no silent drift,
no accidental field addition.
"""
from __future__ import annotations

from dataclasses import dataclass
from typing import Annotated, Literal

from pydantic import BaseModel, ConfigDict, Field, TypeAdapter


# --- Suggestion contract (LLM-04) ---


class Suggestion(BaseModel):
    """A single grounded reply suggestion the agent may read aloud."""

    model_config = ConfigDict(frozen=True, extra="forbid")

    text: str = Field(..., min_length=1, max_length=280, description="Hebrew suggestion text, ≤ 280 chars")
    confidence: float = Field(..., ge=0.0, le=1.0, description="Self-reported model confidence 0.0-1.0")
    citation_ids: list[str] = Field(
        default_factory=list,
        description="Stable chunk IDs from RAG retrieval; empty list = no grounding",
    )


class SuggestionResponse(BaseModel):
    """Final assembled output the engine validates against (LLM-04)."""

    model_config = ConfigDict(frozen=True, extra="forbid")

    suggestions: list[Suggestion] = Field(..., min_length=1, max_length=3)


# --- Streaming event union (LLM-02) ---


class TokenEvent(BaseModel):
    """Streamed token delta. Phase 5 forwards to Phase 6 UI for typewriter rendering."""

    model_config = ConfigDict(frozen=True, extra="forbid")
    type: Literal["token"] = "token"
    delta: str


class CompleteEvent(BaseModel):
    """Final parsed structured output, plus TTFT/total wall-clock."""

    model_config = ConfigDict(frozen=True, extra="forbid")
    type: Literal["complete"] = "complete"
    suggestions: list[Suggestion]
    ttft_ms: int  # -1 sentinel if no token ever arrived (error path)
    total_ms: int
    model: str  # which model actually served (dictalm3 / qwen2.5:7b)


class LlmErrorEvent(BaseModel):
    """Typed error envelope; Phase 5 maps onto WS error frames.

    The ``code`` Literal allowlist is intentionally narrow (4 values).
    Adding a 5th requires plan amendment so consumer switches stay total.
    """

    model_config = ConfigDict(frozen=True, extra="forbid")
    type: Literal["error"] = "error"
    code: Literal["ollama_unreachable", "parse_error", "timeout", "no_context"]
    detail: str


SuggestionEvent = Annotated[
    TokenEvent | CompleteEvent | LlmErrorEvent,
    Field(discriminator="type"),
]
"""Discriminated union of all streaming events. Use ``TypeAdapter(SuggestionEvent)`` for parse."""

# Pre-construct the TypeAdapter so consumers don't have to:
SuggestionEventAdapter: TypeAdapter[TokenEvent | CompleteEvent | LlmErrorEvent] = TypeAdapter(
    SuggestionEvent
)


# --- Context chunk (Phase 4 forward-decl) ---


@dataclass(frozen=True)
class ChunkRef:
    """A retrieved context chunk passed into ``generate_suggestions``.

    Phase 3 owns the contract; Phase 4 RAG re-exports under
    ``receptra.rag.types`` once the retriever is built.

    The ``source`` dict is opaque to the LLM — it carries filename/offset
    metadata for the Phase 6 UI citation chips and is not rendered into the
    prompt (only ``id`` + ``text`` are; see ``receptra.llm.prompts``).
    """

    id: str
    text: str
    source: dict[str, str] | None = None


__all__ = [
    "ChunkRef",
    "CompleteEvent",
    "LlmErrorEvent",
    "Suggestion",
    "SuggestionEvent",
    "SuggestionEventAdapter",
    "SuggestionResponse",
    "TokenEvent",
]
```

Create `backend/tests/llm/test_schema.py` with tests covering every behavior bullet above. At minimum:

```python
"""Schema invariants + discriminated-union round-trip (Plan 03-02)."""
from __future__ import annotations

import pytest
from pydantic import ValidationError

from receptra.llm.schema import (
    ChunkRef,
    CompleteEvent,
    LlmErrorEvent,
    Suggestion,
    SuggestionEvent,
    SuggestionEventAdapter,
    SuggestionResponse,
    TokenEvent,
)


# Suggestion ----------------------------------------------------------------


def test_suggestion_accepts_valid_hebrew() -> None:
    s = Suggestion(text="שלום", confidence=0.9, citation_ids=["kb-1"])
    assert s.text == "שלום"
    assert s.confidence == 0.9


def test_suggestion_rejects_empty_text() -> None:
    with pytest.raises(ValidationError):
        Suggestion(text="", confidence=0.5, citation_ids=[])


def test_suggestion_rejects_too_long_text() -> None:
    with pytest.raises(ValidationError):
        Suggestion(text="x" * 281, confidence=0.5, citation_ids=[])


@pytest.mark.parametrize("bad_conf", [-0.01, 1.01, 1.5])
def test_suggestion_rejects_confidence_out_of_range(bad_conf: float) -> None:
    with pytest.raises(ValidationError):
        Suggestion(text="x", confidence=bad_conf, citation_ids=[])


def test_suggestion_rejects_extra_fields() -> None:
    with pytest.raises(ValidationError):
        Suggestion.model_validate({"text": "x", "confidence": 0.5, "citation_ids": [], "extra": True})


def test_suggestion_is_frozen() -> None:
    s = Suggestion(text="x", confidence=0.5, citation_ids=[])
    with pytest.raises(ValidationError):
        s.text = "y"  # type: ignore[misc]


# SuggestionResponse --------------------------------------------------------


def test_suggestion_response_accepts_one_to_three() -> None:
    SuggestionResponse(suggestions=[Suggestion(text="a", confidence=0.5, citation_ids=[])])
    SuggestionResponse(
        suggestions=[Suggestion(text=str(i), confidence=0.5, citation_ids=[]) for i in range(3)]
    )


def test_suggestion_response_rejects_zero() -> None:
    with pytest.raises(ValidationError):
        SuggestionResponse(suggestions=[])


def test_suggestion_response_rejects_four_plus() -> None:
    with pytest.raises(ValidationError):
        SuggestionResponse(
            suggestions=[Suggestion(text=str(i), confidence=0.5, citation_ids=[]) for i in range(4)]
        )


def test_validate_json_round_trips_hebrew_byte_exact() -> None:
    payload = '{"suggestions":[{"text":"שלום עולם","confidence":0.9,"citation_ids":["kb-1"]}]}'
    parsed = SuggestionResponse.model_validate_json(payload)
    assert parsed.suggestions[0].text == "שלום עולם"
    # round-trip check
    out = parsed.model_dump_json()
    assert "שלום עולם" in out


def test_validate_json_rejects_extra_field() -> None:
    payload = '{"suggestions":[{"text":"x","confidence":0.5,"citation_ids":[],"extra":1}]}'
    with pytest.raises(ValidationError):
        SuggestionResponse.model_validate_json(payload)


# Event union ---------------------------------------------------------------


def test_token_event_discriminator() -> None:
    ev = TypeAdapter_validate({"type": "token", "delta": "שלום"})
    assert isinstance(ev, TokenEvent)
    assert ev.delta == "שלום"


def test_complete_event_discriminator() -> None:
    ev = TypeAdapter_validate({
        "type": "complete",
        "suggestions": [{"text": "x", "confidence": 0.5, "citation_ids": []}],
        "ttft_ms": 312,
        "total_ms": 1845,
        "model": "dictalm3",
    })
    assert isinstance(ev, CompleteEvent)
    assert ev.ttft_ms == 312


def test_error_event_code_literal_allowlist() -> None:
    for code in ("ollama_unreachable", "parse_error", "timeout", "no_context"):
        ev = LlmErrorEvent(code=code, detail="x")  # type: ignore[arg-type]
        assert ev.code == code
    with pytest.raises(ValidationError):
        LlmErrorEvent(code="random", detail="x")  # type: ignore[arg-type]


def test_event_adapter_rejects_unknown_type() -> None:
    with pytest.raises(ValidationError):
        SuggestionEventAdapter.validate_python({"type": "unknown", "x": 1})


# helper to keep tests readable
def TypeAdapter_validate(payload: dict[str, object]) -> SuggestionEvent:  # type: ignore[valid-type]
    return SuggestionEventAdapter.validate_python(payload)


# ChunkRef ------------------------------------------------------------------


def test_chunkref_frozen_with_optional_source() -> None:
    c = ChunkRef(id="kb-1", text="שלום")
    assert c.source is None
    with pytest.raises(Exception):  # FrozenInstanceError from dataclass
        c.id = "kb-2"  # type: ignore[misc]
```
  </action>
  <verify>
    <automated>cd backend && uv run ruff check src/receptra/llm tests/llm && uv run mypy src/receptra/llm tests/llm && uv run pytest tests/llm/test_schema.py -x -v</automated>
  </verify>
  <done>
- All schema tests pass; round-trip preserves Hebrew bytes exactly.
- `LlmErrorEvent.code` Literal rejects unknown values at validation time.
- `SuggestionEventAdapter` rejects unknown discriminators.
- ruff + mypy strict clean on the new package + tests.
- `from receptra.llm import Suggestion, ChunkRef, SuggestionEvent` works at the package boundary.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: receptra.llm.prompts — SYSTEM_PROMPT_HE/EN + FEW_SHOTS_HE + build_user_message + build_messages with DoS bounds</name>
  <files>backend/src/receptra/llm/prompts.py, backend/tests/llm/test_prompts.py</files>
  <behavior>
    - `SYSTEM_PROMPT_HE` is a string identical (byte-for-byte after `.rstrip()`) to the locked content in `<interfaces>`. A test asserts the literal Hebrew refusal phrase `"אין לי מספיק מידע"` appears verbatim AND the JSON shape literal `{"suggestions":[{"text":"..."` appears verbatim.
    - `SYSTEM_PROMPT_EN` is the placeholder English variant from `<interfaces>` (Phase 7 will tune; we ship a faithful translation).
    - `FEW_SHOTS_HE` is `list[dict[str, str]]` with exactly 4 entries: roles user, assistant, user, assistant. Assistant entries are valid JSON (`json.loads(asst["content"])` round-trips to a dict containing a `suggestions` key — proves the few-shot is itself a valid SuggestionResponse).
    - `build_user_message(transcript, [])` returns a string containing `<context>\n(אין קטעי הקשר זמינים)\n</context>` (literal Hebrew "no context blocks available") + `<transcript>\n{transcript}\n</transcript>`.
    - `build_user_message(transcript, [ChunkRef(id="kb-1", text="A"), ChunkRef(id="kb-2", text="B")])` produces a string containing `[id: kb-1]\nA` AND `[id: kb-2]\nB` separated by exactly one blank line, all wrapped in <context> + <transcript> tags.
    - `build_user_message` raises ValueError when `len(transcript) > 2000` (DoS guard); the error message references "transcript exceeds 2000 chars (DoS guard)".
    - `build_user_message` raises ValueError when `len(context_chunks) > 10`; references "more than 10 context chunks".
    - `build_user_message` raises ValueError when `sum(len(c.text)) > 12000`; references "context body exceeds 12000 chars".
    - `build_messages(transcript, chunks, lang='he')` returns a `list[dict[str,str]]` of length 6: [system, user, asst, user, asst, user]. The first message has `role='system'` and `content == SYSTEM_PROMPT_HE`. The last has `role='user'` and content matches `build_user_message(transcript, chunks)` byte-exact.
    - `build_messages(transcript, chunks, lang='en')` substitutes SYSTEM_PROMPT_EN; few-shots stay Hebrew (the few-shot examples are demonstration-only and Hebrew is what the model needs to learn the JSON shape from).
    - `build_messages(transcript, chunks, lang='unknown')` raises ValueError listing the supported langs (he/en).
  </behavior>
  <action>
Create `backend/src/receptra/llm/prompts.py`:

```python
"""Hebrew system prompt + few-shot turns + user-message renderer (LLM-03 + LLM-04).

Every string in this module is LOCKED content from 03-RESEARCH §5.2 + §5.4.
Phase 7 prompt-tuning is the only owner allowed to modify these strings.

DoS bounds (03-RESEARCH §Security Domain):
- transcript ≤ 2000 chars
- ≤ 10 context chunks
- total chunk text ≤ 12000 chars
``build_user_message`` raises ValueError BEFORE any LLM call when these
bounds are violated.
"""
from __future__ import annotations

from typing import Final

from receptra.llm.schema import ChunkRef


SYSTEM_PROMPT_HE: Final[str] = """אתה עוזר וירטואלי לסוכן שירות לקוחות בשיחה טלפונית בעברית.
תפקידך: לקרוא את התמלול של מה שהלקוח אמר ולהציע לסוכן עד שלוש תשובות קצרות, מדויקות, ובסגנון אנושי בעברית.

חוקים מחייבים:
1. השתמש אך ורק במידע שמופיע בקטעי הידע המסומנים <context>...</context>. אל תמציא עובדות עסקיות.
2. אם המידע ב-<context> אינו מספיק כדי לענות, החזר תשובה אחת בלבד עם הטקסט "אין לי מספיק מידע" וציטוטים ריקים.
3. כל תשובה חייבת להיות עד 280 תווים, בעברית טבעית, מנוסחת כמו אדם אמיתי.
4. החזר את התשובה כ-JSON תקין בלבד, ללא טקסט נוסף לפני או אחרי, בפורמט:
{"suggestions":[{"text":"...","confidence":0.0,"citation_ids":["..."]}]}
5. citation_ids חייב להכיל את ה-id המדויק של קטע ה-<context> שעליו התשובה מבוססת.
6. confidence הוא מספר בין 0.0 ל-1.0 שמשקף עד כמה הקטעים מספקים מענה ישיר."""


SYSTEM_PROMPT_EN: Final[str] = """You are a virtual assistant for a customer-service agent on a Hebrew phone call.
Your task: read the transcript of what the customer said and propose up to three short, accurate, human-sounding Hebrew replies for the agent.

Mandatory rules:
1. Use ONLY information appearing inside <context>...</context> blocks. Do not fabricate business facts.
2. If <context> is insufficient, return exactly one suggestion with text "אין לי מספיק מידע" and empty citation_ids.
3. Each suggestion must be ≤ 280 characters, in natural Hebrew, phrased like a real person.
4. Return ONLY valid JSON, with no text before or after, in the format:
{"suggestions":[{"text":"...","confidence":0.0,"citation_ids":["..."]}]}
5. citation_ids MUST contain the exact id of the <context> block your suggestion is grounded in.
6. confidence is a 0.0-1.0 float reflecting how directly the chunks answer the question."""


_FEW_SHOT_USER_1: Final[str] = """<context>
[id: kb-policy-returns]
מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.
</context>

<transcript>
תוך כמה זמן אני יכול להחזיר מוצר?
</transcript>"""

_FEW_SHOT_ASST_1: Final[str] = (
    '{"suggestions":[{"text":"ניתן להחזיר את המוצר תוך 14 ימים מיום הקנייה, '
    'ויש להציג את החשבונית המקורית.","confidence":0.95,"citation_ids":["kb-policy-returns"]}]}'
)

_FEW_SHOT_USER_2: Final[str] = """<context>
[id: kb-policy-returns]
מדיניות החזרים: ניתן להחזיר מוצר תוך 14 יום מיום הקנייה עם החשבונית המקורית.
</context>

<transcript>
מה שעות הפעילות של החנות?
</transcript>"""

_FEW_SHOT_ASST_2: Final[str] = (
    '{"suggestions":[{"text":"אין לי מספיק מידע","confidence":0.0,"citation_ids":[]}]}'
)


FEW_SHOTS_HE: Final[list[dict[str, str]]] = [
    {"role": "user", "content": _FEW_SHOT_USER_1},
    {"role": "assistant", "content": _FEW_SHOT_ASST_1},
    {"role": "user", "content": _FEW_SHOT_USER_2},
    {"role": "assistant", "content": _FEW_SHOT_ASST_2},
]


# DoS bounds (03-RESEARCH §Security Domain)
MAX_TRANSCRIPT_CHARS: Final[int] = 2000
MAX_CONTEXT_CHUNKS: Final[int] = 10
MAX_CONTEXT_BODY_CHARS: Final[int] = 12000

_EMPTY_CONTEXT_MARKER: Final[str] = "<context>\n(אין קטעי הקשר זמינים)\n</context>"


def build_user_message(transcript: str, context_chunks: list[ChunkRef]) -> str:
    """Render a single user message with <context>...</context> + <transcript>...</transcript>.

    Raises:
        ValueError: when DoS bounds are exceeded (pre-LLM guard).
    """
    if len(transcript) > MAX_TRANSCRIPT_CHARS:
        raise ValueError(
            f"transcript exceeds 2000 chars (DoS guard, see 03-RESEARCH §Security Domain): "
            f"got {len(transcript)} chars"
        )
    if len(context_chunks) > MAX_CONTEXT_CHUNKS:
        raise ValueError(
            f"more than 10 context chunks (DoS guard): got {len(context_chunks)}"
        )
    body_chars = sum(len(c.text) for c in context_chunks)
    if body_chars > MAX_CONTEXT_BODY_CHARS:
        raise ValueError(
            f"context body exceeds 12000 chars (DoS guard): got {body_chars}"
        )

    if not context_chunks:
        ctx_block = _EMPTY_CONTEXT_MARKER
    else:
        rendered = "\n\n".join(f"[id: {c.id}]\n{c.text}" for c in context_chunks)
        ctx_block = f"<context>\n{rendered}\n</context>"
    return f"{ctx_block}\n\n<transcript>\n{transcript}\n</transcript>"


def build_messages(
    transcript: str,
    context_chunks: list[ChunkRef],
    lang: str = "he",
) -> list[dict[str, str]]:
    """Build the full ChatML message list: system + 2 few-shot turns + final user.

    Args:
        transcript: customer utterance (≤ 2000 chars)
        context_chunks: retrieved RAG chunks (≤ 10 entries, ≤ 12000 chars total body)
        lang: 'he' (default, Hebrew system prompt) or 'en' (Phase 7 A/B fallback)

    Returns:
        list[dict] of length 6: [system, user, asst, user, asst, user]
    """
    if lang == "he":
        system = SYSTEM_PROMPT_HE
    elif lang == "en":
        system = SYSTEM_PROMPT_EN
    else:
        raise ValueError(f"unsupported lang={lang!r}; expected 'he' or 'en'")

    user_content = build_user_message(transcript, context_chunks)
    return [
        {"role": "system", "content": system},
        *FEW_SHOTS_HE,
        {"role": "user", "content": user_content},
    ]


__all__ = [
    "FEW_SHOTS_HE",
    "MAX_CONTEXT_BODY_CHARS",
    "MAX_CONTEXT_CHUNKS",
    "MAX_TRANSCRIPT_CHARS",
    "SYSTEM_PROMPT_EN",
    "SYSTEM_PROMPT_HE",
    "build_messages",
    "build_user_message",
]
```

Create `backend/tests/llm/test_prompts.py` covering every behavior bullet:

```python
"""Prompt-builder structure + Hebrew byte-exact preservation + DoS bounds (Plan 03-02)."""
from __future__ import annotations

import json

import pytest

from receptra.llm.prompts import (
    FEW_SHOTS_HE,
    SYSTEM_PROMPT_EN,
    SYSTEM_PROMPT_HE,
    build_messages,
    build_user_message,
)
from receptra.llm.schema import ChunkRef, SuggestionResponse


# System prompt -------------------------------------------------------------


def test_system_prompt_he_contains_canonical_refusal() -> None:
    assert "אין לי מספיק מידע" in SYSTEM_PROMPT_HE


def test_system_prompt_he_contains_json_shape_literal() -> None:
    assert '{"suggestions":[{"text":"..."' in SYSTEM_PROMPT_HE


def test_system_prompt_en_contains_refusal_in_hebrew_quotes() -> None:
    # English prompt MUST instruct the model to refuse in Hebrew, not English
    assert "אין לי מספיק מידע" in SYSTEM_PROMPT_EN


# Few-shot ------------------------------------------------------------------


def test_few_shots_alternating_roles() -> None:
    assert len(FEW_SHOTS_HE) == 4
    assert [m["role"] for m in FEW_SHOTS_HE] == ["user", "assistant", "user", "assistant"]


def test_few_shot_assistants_are_valid_suggestion_response() -> None:
    for i in (1, 3):  # asst entries at index 1 and 3
        payload = FEW_SHOTS_HE[i]["content"]
        # Must be parseable JSON
        json.loads(payload)
        # Must be a valid SuggestionResponse
        SuggestionResponse.model_validate_json(payload)


def test_few_shot_2_is_canonical_refusal() -> None:
    parsed = SuggestionResponse.model_validate_json(FEW_SHOTS_HE[3]["content"])
    assert parsed.suggestions[0].text == "אין לי מספיק מידע"
    assert parsed.suggestions[0].confidence == 0.0
    assert parsed.suggestions[0].citation_ids == []


# build_user_message --------------------------------------------------------


def test_build_user_message_empty_chunks_uses_hebrew_marker() -> None:
    out = build_user_message("שלום", [])
    assert "(אין קטעי הקשר זמינים)" in out
    assert "<transcript>\nשלום\n</transcript>" in out


def test_build_user_message_renders_inline_id_markers() -> None:
    chunks = [
        ChunkRef(id="kb-1", text="טקסט א"),
        ChunkRef(id="kb-2", text="טקסט ב"),
    ]
    out = build_user_message("שלום", chunks)
    assert "[id: kb-1]\nטקסט א" in out
    assert "[id: kb-2]\nטקסט ב" in out
    # blank-line separator between chunks
    assert "טקסט א\n\n[id: kb-2]" in out


def test_build_user_message_rejects_oversize_transcript() -> None:
    with pytest.raises(ValueError, match="transcript exceeds 2000"):
        build_user_message("x" * 2001, [])


def test_build_user_message_rejects_too_many_chunks() -> None:
    chunks = [ChunkRef(id=f"k{i}", text="x") for i in range(11)]
    with pytest.raises(ValueError, match="more than 10 context chunks"):
        build_user_message("שלום", chunks)


def test_build_user_message_rejects_oversize_context_body() -> None:
    chunks = [ChunkRef(id="big", text="x" * 12001)]
    with pytest.raises(ValueError, match="context body exceeds 12000"):
        build_user_message("שלום", chunks)


def test_build_user_message_accepts_at_bounds() -> None:
    # 2000-char transcript + 10 chunks summing to 12000 chars all PASS
    chunks = [ChunkRef(id=f"k{i}", text="x" * 1200) for i in range(10)]
    out = build_user_message("y" * 2000, chunks)
    assert "[id: k0]" in out
    assert len(out) > 12000


# build_messages ------------------------------------------------------------


def test_build_messages_he_returns_six_messages() -> None:
    msgs = build_messages("שלום", [], lang="he")
    assert len(msgs) == 6
    assert msgs[0] == {"role": "system", "content": SYSTEM_PROMPT_HE}
    assert msgs[-1]["role"] == "user"
    assert msgs[-1]["content"] == build_user_message("שלום", [])


def test_build_messages_en_swaps_only_system() -> None:
    msgs_he = build_messages("שלום", [], lang="he")
    msgs_en = build_messages("שלום", [], lang="en")
    assert msgs_en[0]["content"] == SYSTEM_PROMPT_EN
    # Few-shots remain Hebrew demonstrations
    assert msgs_he[1] == msgs_en[1]
    assert msgs_he[2] == msgs_en[2]


def test_build_messages_rejects_unknown_lang() -> None:
    with pytest.raises(ValueError, match="unsupported lang"):
        build_messages("שלום", [], lang="fr")
```
  </action>
  <verify>
    <automated>cd backend && uv run ruff check src/receptra/llm tests/llm && uv run mypy src/receptra/llm tests/llm && uv run pytest tests/llm/test_prompts.py -x -v</automated>
  </verify>
  <done>
- All prompts tests pass (Hebrew byte-exact preservation + DoS bounds + alternating roles).
- `SYSTEM_PROMPT_HE` byte-exactly matches the RESEARCH §5.2 lock.
- Few-shot assistant entries parse cleanly as SuggestionResponse — proves the few-shot is itself a valid demonstration.
- DoS guards raise ValueError BEFORE any I/O.
- ruff + mypy strict clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → schema validation | Untrusted JSON from Ollama crosses here; pydantic v2 validates with extra='forbid' |
| caller → prompts.build_user_message | Untrusted transcript + chunk text crosses here; DoS bounds enforced pre-LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | T (Tampering) | Suggestion model accepts unbounded text | mitigate | `min_length=1, max_length=280` on `Suggestion.text` (RESEARCH §5.2 rule 3); `extra='forbid'` rejects injected fields |
| T-03-02-02 | D (Denial of service) | build_user_message accepting arbitrarily long input | mitigate | Pre-LLM bounds: transcript ≤ 2000 chars, ≤ 10 chunks, ≤ 12000 chars body. ValueError raised BEFORE Ollama call (Plan 03-04 catches and yields LlmErrorEvent). |
| T-03-02-03 | T (Tampering) | LlmErrorEvent.code accepting arbitrary string | mitigate | Literal allowlist of 4 values; pydantic ValidationError on unknown |
| T-03-02-04 | I (Information disclosure) | Few-shot examples leaking real customer data | accept | Few-shots use synthetic policy text ("מדיניות החזרים" generic example). No PII in static prompts. |
| T-03-02-05 | T (Tampering) | Prompt injection via context chunk text ("ignore previous instructions...") | accept | Industry consensus 2026: prompt injection has no general defense. RESEARCH §Security Domain documents this. Phase 7 polish revisits. The hard short-circuit on empty context (Plan 03-04) bypasses the LLM entirely for the highest-risk path. |
| T-03-02-06 | T (Tampering) | Hebrew text byte corruption through serialization | mitigate | `test_validate_json_round_trips_hebrew_byte_exact` regression-guards UTF-8 preservation through pydantic serialize/deserialize round-trip |
</threat_model>

<verification>
- `cd backend && uv run pytest tests/llm/test_schema.py tests/llm/test_prompts.py -x -v` green (~25 tests).
- `from receptra.llm import Suggestion, ChunkRef, SuggestionEvent` succeeds at the package boundary.
- `from receptra.llm.prompts import build_messages` returns 6-element list with Hebrew byte-exact preservation.
- ruff + mypy strict clean across `backend/src/receptra/llm/__init__.py`, `schema.py`, `prompts.py` and matching tests.
- `grep -F 'אין לי מספיק מידע' backend/src/receptra/llm/prompts.py` finds the canonical refusal in BOTH `SYSTEM_PROMPT_HE`, `SYSTEM_PROMPT_EN`, and `FEW_SHOTS_HE` (3 hits minimum) — structural guarantee that the grounding contract is not silently weakened.
</verification>

<success_criteria>
- LLM-04 schema contract published (Suggestion, SuggestionResponse, SuggestionEvent discriminated union).
- LLM-03 grounding pattern locked at the prompt level (Hebrew system prompt + 2 few-shot turns containing the canonical refusal).
- DoS bounds (T-03-02-02) enforced pre-LLM at build_user_message; engine layer (Plan 03-04) inherits the guard.
- Plan 03-03 (client) and Plan 03-04 (engine) can import everything they need from `receptra.llm` and `receptra.llm.prompts` without further wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hebrew-suggestion-llm/03-02-SUMMARY.md` covering:
- Final byte-exact SHA256 of `SYSTEM_PROMPT_HE` for downstream regression watch.
- Hebrew round-trip test result (proves UTF-8 preservation on the executor's filesystem).
- Confirmation that Plans 03-03 + 03-04 inherit the schema/prompt surface unchanged.
</output>
